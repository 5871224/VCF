# VCF 分析工具正式規格

唯一正式網址：`https://5871224.github.io/VCF/`

本文件是 VCF 工作台、搜尋引擎、題庫與題目產生器的**唯一產品規格**。只保留目前正式行為，不保留修改歷程、舊版流程、已淘汰方案或過時預設值。

禁手遞迴與棋型證明的實作細節另見 [`禁手判斷規格.MD`](禁手判斷規格.MD)。

## 1. 產品範圍

本工具提供 15×15 連珠／五子棋盤面的：

- 手動編輯與圖片匯入。
- 黑方或白方單組、多組與最短 VCF 搜尋。
- VCF 防守、全部 VCF 防守與候選點分析。
- 補黑／補白後逐點搜尋 VCF。
- 有禁規則下的黑棋禁手顯示。
- Supabase 題庫。
- 指定 1～10 步的 VCF 題目產生器。

本工具不是完整對局 AI；「VCT 點」只代表候選點分析，不代表已完成完整 VCT 數學證明。

## 2. 正式入口與架構

### 2.1 根工作台

```text
/VCF/index.html（由 makevcf.html 建置）
  → rapfi/engine/vcf-bitboard-engine.js / .wasm
  → rapfi/vcf-bitboard-main.js
  → rapfi/vcf-bitboard-worker.js
```

規則：

- 根網址直接顯示新版 Bitboard 工作台，不轉址到 `/rapfi/`。
- `makevcf.html` 是根頁建置來源，不是第二套正式產品頁。
- `/rapfi/` 只放置引擎、Worker、UI 模組與實驗室資源。
- 正式介面與腳本只維護一份來源，不保留另一套舊版工作台。

### 2.2 題目產生器

```text
makevcf-generator-core.js
  → rapfi/vcf-bitboard-worker.js
  → rapfi/engine/vcf-bitboard-engine.js / .wasm
```

- 題目產生器與主棋盤共用盤面，但使用自己的 Worker。
- 題目產生器不得共用、中止或重新啟動主分析 Worker。
- 停止產生時立即終止題目產生器 Worker；新 Worker 在背景重新初始化，不阻塞停止按鈕。
- 模板枚舉使用頁面提供的同步棋型與禁手函式；根工作台由 `rapfi/vcf-bitboard-generator-compat.js` 提供相容層。
- 題目產生器、工作台 UI、回放與題庫腳本由 `makevcf.html` 依單一固定順序載入；功能模組不得再以動態 `<script>` 注入其他正式模組。
- Pages 建置只能驗證正式來源，不得在部署時改寫 `makevcf.html`、題目產生器、Evaluator 或回放程式。

### 2.3 主要檔案

| 檔案 | 用途 |
|---|---|
| `makevcf.html` | 15×15 工作台、共用控制項與建置來源 |
| `rapfi/vcf-bitboard-main.js` | 主執行緒 Wasm、Worker 與頁面服務 |
| `rapfi/vcf-bitboard-worker.js` | Worker RPC 與 Wasm 呼叫 |
| `rapfi/rapfi-bitboard-dashboard.js` | 搜尋、補子與工作台操作 |
| `rapfi/vcf-shortest-vcf-ui.js` | 最短 VCF 操作與證明狀態 |
| `rapfi/vcf-forbidden-overlay.js` | 禁手圖層 |
| `rapfi/rapfi-question-bank.js` | Supabase 題庫 |
| `makevcf-generator-*.js` | 題目產生器各階段 |
| `rapfi/vcf-pattern-engine-optimized.cpp` | 棋型、長連與禁手核心 |
| `rapfi/vcf-bitboard-search-v2.cpp` 與 `.inc` | VCF 搜尋核心 |

完整檔案分工、載入關係及生成產物見 [`檔案用途總覽.MD`](檔案用途總覽.MD)。

## 3. 棋盤、路線與規則

### 3.1 棋盤

- 棋盤固定 15×15，共 225 個交點。
- 索引：`idx = row × 15 + col`。
- `0 = 空點`、`1 = 黑棋`、`2 = 白棋`。
- 引擎可使用長度 226 的相容陣列，第 225 格為盤外值。
- VCF 路線是索引陣列，第一手為攻方，之後攻守交替。
- 主棋盤可保存於 `localStorage`；題庫不得使用 `localStorage`。

### 3.2 規則值

| 值 | 名稱 | 長連與禁手 | 棋型範圍 |
|---:|---|---|---|
| `0` | 自由 | 黑白長連皆算勝，無禁手 | 黑白皆取 9 格 |
| `1` | 無禁 | 黑白長連不算勝也不算負，無禁手 | 黑白皆取 11 格 |
| `2` | 有禁 | 連珠；黑棋檢查長連、四四、三三，白棋長連算勝 | 黑棋 11 格、白棋 9 格 |

每個 Worker 搜尋、防守、精簡與棋型請求都必須明確附帶規則值。不得使用 `Number(rules) || 2` 等會把自由規則 `0` 誤判成預設值的寫法。

## 4. VCF 搜尋核心

### 4.1 `single`

- 用於最快找到一組 VCF。
- 找到第一組立即返回。
- 使用專用精確同型表。
- 不列舉全部路線。
- 預設最大深度 200 ply。

### 4.2 `multi`

- 在指定 `maxDepth` 內列舉多組 VCF。
- 最多回傳 64 組。
- 固定精簡代表路線。
- 達組數、時間或節點限制時保留已找到結果，並回傳中止狀態。

### 4.3 `shortest`

- 逐一搜尋合法的奇數 ply。
- 第一個有解深度即為目前最短層。
- 回傳最短層中保留下來的多組路線。
- 因限制中止時，不得宣告已完整列出最短層。

### 4.4 `shortest-one`

- 全程使用單一 Worker、單執行緒。
- 先找目前最佳路線，再以 `bestPly - 2` 排除更短路線。
- 找到更短反例後更新上限並繼續。
- 只有完整排除全部更短範圍時，才設定 `shortestProven = true`。
- 因限制中止時可保留目前最佳路線，但介面必須顯示「尚未證明為最短」。

## 5. 多組剪枝與路線去重

剪枝選單：

```text
高速多組 VCF（集合子集） → fast
嚴格多組 VCF（完全相同） → strict
```

### 5.1 搜尋狀態剪枝

- `strict`：只使用完整黑白盤面完全相同的無解狀態剪枝。
- `fast`：在完整同盤剪枝外，可使用已完整證明的勝型集合子集剪枝。
- 未完整搜尋、限制中止或尚未證明的狀態不得寫入勝型子集表。

### 5.2 多組輸出去重

每條保留完整手順供顯示、回放及 `getBlockVCF()` 使用；正規化只用於比較：

- 多手 VCF 的最後一手若形成攻方活四，去重比較時移除該尾手。
- 第一手就是活四時不得退回空集合，必須保留活四落子點。
- `strict`：正規化後攻方集合與守方集合都完全相同才合併。
- `fast`：正規化後攻方集合與守方集合分別構成子集時，只保留較小集合。
- 防守點覆蓋數必須使用去重後的組數計算。

工作台若另提供「同型－活四取前一手」顯示選項，只能影響畫面上的同型整理；不得截短代表路線或改變 VCF 合法性。

### 5.3 活四不搜後續

「活四不搜後續」是可選的節點內候選支配剪枝：

- 預設不勾選，設定鍵為 `vcf_stop_after_open_four`。
- 適用於 `multi`、`shortest`，以及題目產生器使用這兩種模式時。
- 不套用於 `single`、`shortest-one`、路線驗證、精簡與防守計算。
- 同一攻方節點若存在直接連五或不可被單點防守的活四，只保留該節點全部直接五與活四終局候選，不再展開後續單防死四。
- 不得只保留第一個活四；同一節點的全部終局勝點仍要列舉。
- 搜尋中停用此選項；`fast`、`strict` 共用同一設定。

## 6. 時間、節點與狀態

工作台提供：

- 時間限制：秒。
- 節點限制：百萬節點。
- `0` 表示該項不限制。

多組型搜尋使用 32-bit `maxNode` 封裝：

```text
bit 31      ：封裝標記
bit 30      ：活四不搜後續
bit 10..29  ：時間上限，單位秒；0 = 不限制
bit 0..9    ：節點上限，單位百萬；0 = 不限制
```

時間欄位最大值為 1,048,575 秒。舊橋接的 `0xffffffff` 不限節點值在啟用其他封裝選項前，必須先正規化成新版格式。

規則：

- 多組、最短、補子搜尋與題目產生器共用這兩個輸入。
- 不得以固定 500 萬節點覆蓋使用者輸入。
- 搜尋時間每 524,288 節點檢查一次。
- 達限制時可保留部分結果，但必須明確標示搜尋未完整。
- 未完整時不得把「目前未找到」、「目前最短」或「目前沒有其他路線」描述為完整證明。
- 搜尋期間停用會改變搜尋語意的控制項。

完成提示格式：

```text
7.4351 秒，30,123,123 節點，4,123,123 節點/秒
```

## 7. 工作台功能

### 7.1 棋盤操作

- 使用 SVG 顯示棋盤與星位。
- 點擊空點依下一手顏色落子；點擊已有棋子則移除。
- 盤面或規則改變後，清除失效路線、分析、題目答案與 N 點，並重新計算禁手。
- 「清除路線／分析」不移除棋子。
- 「清空棋盤」移除全部棋子與衍生狀態。

### 7.2 基本搜尋

- 黑 VCF／白 VCF 使用 `single`。
- 多組 VCF 使用 `multi`，提供上一組／下一組切換。
- 最短 VCF 使用 `shortest-one`，區分已證明最短與目前最佳。
- 「速找 VCF」與「最短 VCF」按鈕文字為黑色。

### 7.3 防守與候選點

- 「VCF 防守」針對目前顯示的完整路線取得直接防守點。
- 路線防守使用產生該路線時記錄的攻方顏色，不得改用目前分析色。
- 「全部 VCF 防守」取得候選後逐點補守，再重新搜尋；補後仍有 VCF 的點不是有效防點。
- 「VCT 點」先檢查既有 VCF；沒有 VCF 時才逐點顯示連五、四與 VCF 候選。

### 7.4 補子搜尋

下拉選單只保留：

- 「最速」：每個補子點使用 `single`。
- 「最短」：每個補子點使用 `shortest-one`。

顯示：

- `5`：補子後直接連五。
- `4`：補子後形成四。
- 其他數字：補子後可形成的 VCF 步數。
- 搜尋完成後，在所有 VCF 數字中圈選數字最大的落點；`4`、`5` 不參與圈選。
- 任一落點達限制時，整體結果標示未完整。

### 7.5 禁手顯示

- 只在有禁規則下顯示黑棋禁點。
- 長連顯示 `6`、四四顯示 `44`、三三顯示 `33`。
- 禁手使用獨立 SVG 圖層，不得被 VCF、補子、防守或一般分析圖層清除。
- 棋型與禁手的完整證明規則以 `禁手判斷規格.MD` 為準。

## 8. 圖片匯入

輸入來源：

- 檔案上傳。
- 桌機貼上截圖。
- 手機相機拍照。

正式流程：

```text
載入圖片
  → 四邊形候選與 15 路格線驗證
  → 可信度不足時改用 Hough 格線候選
  → 透視校正與 15×15 投影評分
  → 使用者拖曳修正
  → 自適應空／黑／白辨識
  → 半透明預覽與逐點人工修正
  → 套用到主棋盤
```

要求：

- 自動定位必須以格線支持度、覆蓋率、連續性及橫直平衡驗證，不能只取最大四邊形。
- 候選四角要吸附到第一及第十五條格線，而不是木板或裝飾外框。
- 預覽保留最外格線外 0.68 格，避免邊線棋子被裁切。
- 棋子中心可能有手數文字，辨識要以棋身環帶與圓周特徵為主。
- 使用者可在預覽中循環修改「空 → 黑 → 白 → 空」。
- 自動定位失敗時提供手動四角框。
- 套用後清除失效的題目、答案、N 點與分析。

## 9. 使用者設定與題庫

### 9.1 使用者設定記憶

正式工作台與題目產生器中，由使用者手動調整的設定使用瀏覽器 `localStorage` 保存並自動恢復，包括：

- 規則、分析色、多組剪枝、時間與節點限制。
- 補子搜尋模式、「活四不搜後續」及其他搜尋勾選項。
- 題目產生器攻方、步數、加成、排序、補子、唯一題及後續動態設定欄位。

要求：

- 單選項保存群組值；勾選項保存狀態；數字、文字、範圍與下拉選單保存目前值。
- 數字恢復仍遵守欄位 `min`、`max`。
- `#rule-box`、`#analysis-box`、`#vcf-search-options`、`#generator-panel` 內後續加入且具有穩定 `id` 或 `name` 的設定自動納入記憶。
- 不保存檔案欄位、隱藏欄位、按鈕、棋盤內容、搜尋結果、答案／N 點顯示狀態或圖片校正暫存資料。
- 動態欄位建立後立即恢復設定；恢復時觸發正常 `change` 流程。
- 同站其他分頁修改設定時，可透過 `storage` 事件同步。

### 9.2 題庫

- 題庫唯一資料來源為 Supabase。
- 只保存 225 格 `board` 與 `attacker`。
- 不保留 `localStorage` 題庫、舊資料搬移或多後端相容層。
- 載入題目時使用目前工作台規則。
- 題庫位於 VCF 題目產生器區塊最下方。
- 支援重新整理、上一題、下一題、題號定位、新增目前盤面與刪除目前題目。

# 第二部：VCF 題目產生器

## 10. 介面與設定

按下「VCF 題目產生器」籤頁後：

- 不再顯示重複的「VCF 題目產生器」區塊標題。
- 「產生 N 步 VCF」、「停止產生」、「顯示答案」、「顯示 N 點」位於區塊最上方。
- 題庫位於區塊最下方。
- 產生中狀態以兩行顯示目前階段、驗證條件與進度。

設定包含：

- 攻方：黑或白。
- 規則：自由、無禁、有禁。
- 生成步數：1～10。
- 沿用棋子加成。
- 棋子集中加成。
- 依加成高低排序。
- 補齊黑白子數。
- 三型加成。
- 只保留目標 VCF。
- 多組剪枝、時間限制與節點限制。

產生開始時取得設定快照；產生期間鎖定棋盤、規則、攻方、步數、搜尋限制、剪枝及權重控制項。

## 11. VCF 步數與標準完成盤面

VCF 步數是最終勝形成立前，攻方實際落子的手數。

```text
攻方死四 → 守方擋 → 攻方活四 → 守方擋 → 攻方連五
```

以上為 2 步 VCF；最後連五不計入。同一手同時形成四四仍只算 1 步。

白方抓禁手中，白方下出最後死四、使黑方唯一防點成為禁手的那一手計入；非法黑方禁手不列入合法答案。

標準完成盤面：

- 四四：四四形成後。
- 活四：攻方下出活四之前。
- 連五：連五形成後。
- 抓禁手：白方下出最終死四後、黑方尚未非法落在禁點 A。
- 延伸驗證比較 225 格黑白位置，不要求代表路線順序完全相同。

## 12. N 點

N 點使用兩個獨立位元：

- `NO_BLACK`：黑方不可使用。
- `NO_WHITE`：白方不可使用。
- 同一點可同時成為雙方 N 點。

顯示：

- 綠色方塊：雙方 N。
- 紅框黑色方塊：黑方 N。
- 紅框白色方塊：白方 N。

完整目標 VCF 路線落點、抓禁手最終禁點 A，以及自動補守的受保護棋子位置，依其用途加入 N 點。

盤上已有棋子與 N 點是兩份獨立資料。自動補守棋仍保留實際棋子，同時將位置標成雙方 N，不會因 N 標記被移除。

新死四候選完成棋型確認後、回傳候選前，必須立即設定本層連五點：

- 一般死四的 `fivePoint` 設為 `NO_BLACK | NO_WHITE`。
- 同線四四的所有 `lineFivePoints` 都設為 `NO_BLACK | NO_WHITE`；重複座標只處理一次。
- 不得改動無關位置的 N 點。
- 後續補守、封鎖其他 VCF、補齊黑白子數與新增外層死四，都使用已更新的 `nMask`，且不得在這些連五點落子。

## 13. 初始材料

### 13.1 活三

```text
留 留 攻 攻 攻 留
留 攻 留 攻 攻 留
```

- 三個「攻」為攻方棋，皆可作 A。
- 「留」為雙方 N 點。
- 活三接一層新死四形成 2 步基礎。

黑方有禁規則增加外側黑方 N：

```text
留 留 攻 攻 攻 留 N
N 留 攻 留 攻 攻 留 N
```

### 13.2 死四

```text
X 攻 攻 攻 攻 留
攻 攻 攻 留 攻
攻 攻 留 攻 攻
```

- `X` 為盤端或守方棋；盤內 X 為攻方 N。
- 指定靠外棋子不得作 A。
- 「留」為雙方 N。
- 死四接一層新死四時，A 下回形成四四，得到 1 步 VCF。
- 有禁規則且黑方進攻時，四四為禁手，因此沒有合法黑方 1 步題。

### 13.3 白方抓禁手

只在 `規則 = 2` 且攻方為白方時使用。一般白方 VCF 與抓禁手題型都保留；進入抓禁題型時，長連、四四、三三等機率選擇。

共同流程：

1. 建立黑棋禁手骨架，禁點記為 A。
2. 移除 A 上的暫時黑棋，A 設為雙方 N。
3. 建立白棋死四，五點固定為 A。
4. 白棋下回後必須形成單一死四。
5. 在黑棋尚未落 A 的盤面，以正式禁手函式確認黑 A 確實為禁手。
6. 禁手類型可以重疊，不要求互斥。

長連模板：

```text
黑 A 黑 黑 黑 黑 X
黑 黑 A 黑 黑 黑 N N
```

四四：

- 可為不同方向或同線雙四。
- 同線雙四必須具有兩個不同五點。
- 建立黑方骨架時，移除 A 後留下的活三不補白棋封閉；此例外只用於抓禁骨架。

三三：

```text
留 留 黑 黑 黑 留 N
N 留 黑 留 黑 黑 留 N
```

取兩個不同方向活三交於 A，移除 A 後再以正式禁手判斷確認。

## 14. 新死四模板與相容規則

模板：

```text
1. X 五 攻 攻 攻 攻 X
2. X 攻 五 攻 攻 攻 X
3. X 攻 攻 五 攻 攻 X
```

- `攻`：攻方棋。
- `五`：死四後守方唯一防點。
- `X`：模板外側。
- A 必須是原盤既有攻方棋，與模板攻位重合。
- 套入模板後移除 A，A 成為新題目的第一手。
- 枚舉四方向與正反方向。

相容規則：

- 攻位可沿用攻方棋，或在非攻方 N 的空點新增；不能覆蓋守方棋。
- A 不能是禁止 A 的棋或攻方 N，且不計入沿用加成。
- 五點不能是攻方 N 或攻方棋；可為空點或原有守方棋。
- 五點原有守方棋可在生成時暫時移除，並計入沿用加成。
- X 可為空點、守方棋、N 點或盤端，不能是攻方棋。
- 抓禁手最內層的五點 A 保持空點，不由黑方合法下回。

新死四移除 A 後若產生活三：

1. 只在左右 X 的可用空點補守方棋。
2. 枚舉左、右、兩端組合。
3. 補後仍有活三的方案淘汰。
4. 只保留補守子數最少的方案。

## 15. 指定步數生成

### 15.1 一般題型

- 1 步：以死四基礎外加一層死四，A 下回合法形成四四。
- 2～10 步：從活三或死四基礎開始，反向逐層增加新死四。
- 每成功增加一層，目標 VCF 步數必須正好增加 1。
- 同層候選失敗時回溯；整個基礎無法延伸時重建基礎。

層數：

```text
活三路線新增死四層數 = 目標步數 - 1
死四路線新增死四層數 = 目標步數
```

### 15.2 抓禁手題型

- 先建立 1 步抓禁手基礎。
- 再外加 `目標步數 - 1` 層新死四。
- 每層重搜並確認步數正好增加 1。
- 必須持續保留禁點 A、禁手類型與正式禁手驗證資料。

## 16. 目標、較短與其他 VCF

每組搜尋結果重播後分類：

- **目標 VCF**：步數等於指定步數，且標準完成盤面等於預期盤面。
- **較短 VCF**：步數小於指定步數。
- **其他 VCF**：不是較短，且標準完成盤面不同於目標盤面。

處理範圍：

- 較短 VCF 永遠是不希望保留的路線。
- 未勾選「只保留目標 VCF」：只處理較短 VCF。
- 已勾選：同時處理較短與其他 VCF。
- 每次補子後至少要保留一組目標 VCF；不要求保留原本完全相同的手順。

## 17. 多組搜尋與自動補守

需要補守時使用 `multi`：

```text
maxVCF = 64
pruning = 產生開始時的剪枝快照
maxDepth = 未勾選只留目標時使用目標深度；
           勾選只留目標時可搜尋到 200 ply
maxNode = 時間／節點限制封裝值
```

流程：

1. 搜尋多組 VCF。
2. 由 C++ 完成活四正規化及 `fast`／`strict` 去重。
3. 依第 16 節篩選需處理的路線。
4. 對每組取得完整手順的防守點。
5. 排除非空點、守方 N、守方形成四／五、黑方禁手等非法位置。
6. 統計每個點可同時封鎖的去重後路線數。
7. 先嘗試覆蓋數最多的點；同數時依棋盤索引排序。
8. 補一顆守方棋後，從新盤面重新執行整次多組搜尋。
9. 目標消失或後續無法清除時回溯，改試下一點。
10. 每個補守候選最多展開 96 個有界遞迴狀態。

若任何一組已知需處理 VCF 沒有合法防守點，該分支失敗。

### 17.1 搜尋達上限

達時間、節點或 64 組上限時：

- 已找到至少一組需處理 VCF：依目前結果先補守，再重新搜尋。
- 完全沒有找到需處理 VCF：視為成功，保留搜尋受限警告。
- 已找到需處理 VCF但沒有合法防點：不能視為成功。
- 搜尋上限表示可能仍有未知路線，但不否定已找到的路線；已知路線必須處理。

### 17.2 補守棋保護

為封鎖較短／其他 VCF 而加入的守方棋：

- 保留在實際盤面。
- 位置立即設成雙方 N。
- 記入受保護補守集合。
- 後續增加死四時，若模板打算把該棋作為五點守方棋而移除，候選直接淘汰。
- 原始盤面本來存在的其他守方棋仍可照模板規則作為五點。

## 18. 補齊黑白子數

只在指定步數完成後依合法輪次差額決定缺少顏色：

- 黑方待下：目標為黑白數相等。
- 白方待下：目標為黑棋比白棋多一顆。
- 可補攻方或守方，取決於盤面實際差額，不固定只補守方。

候選要求：

- 棋盤內圈空點。
- 不是該顏色 N 點。
- 不是目標 VCF 關鍵防守點。
- 補入守方棋不得形成四或五。
- 有禁規則下補黑棋不得為禁手。

每補一顆：

- VCF 搜尋色固定為原題目的攻方，不得改成補入棋子的顏色。
- 重新搜尋並保留指定步數與標準完成盤面。
- 不得出現較短 VCF。
- 補攻方棋時必須繼續搜尋同步數 VCF；不得因第一條最短路線仍是目標就提前接受，也不得新增另一組同步數、不同完成盤面的攻方 VCF。
- 同一標準完成盤面的不同手順不算新增 VCF。
- 分支失敗時撤銷並回溯。

限制：

- 每層最多嘗試 6 個點。
- 最多 32 個唯一盤面狀態。
- 最多 6 輪。
- 整體 12 秒。
- 最短驗證先使用 800,000 節點；需要多組確認時最多 16 組、1,600,000 節點。

補點權重：

- 攻方活四最高。
- 攻方死四次之。
- 攻守三型乘上「三型加成」。
- 再依周圍兩格內棋子數提高集中度。

## 19. 候選權重

### 19.1 沿用棋子加成

- A 不計入。
- 計算新死四另外三個攻位沿用的攻方棋。
- 計算 X 點與五點原有的守方棋。

### 19.2 棋子集中加成

- 以盤面全部黑白棋座標平均值為分布中心。
- 新死四模板中央越接近分布中心，分數越高。

### 19.3 驗證順序

- 未勾選「依加成高低排序」：依權重做加權隨機排序。
- 已勾選：總權重由高到低；同權重隨機。
- 權重只改變嘗試順序，不放寬棋型、N 點、禁手、步數或 VCF 驗證。

## 20. 回放、狀態與結果

### 20.1 統一回放與顯示

- 題目產生器只顯示一張回放卡片；完整生成與逐顆補子共用一條時間軸，不得保留第二張補子回放卡片、第二組導航或第二個計數器。
- 每次開始產生時清除上一輪紀錄；產生期間不即時刷新主棋盤且不能操作回放，成功、失敗或停止後才顯示本輪回放。
- 時間軸依實際順序包含初始活三、初始死四、禁手骨架、每個死四候選、中途補守、最終唯一化、補齊黑白子數與最終題目。
- 初始材料即使沒有產生可驗證候選也要保留；同一驗證分支重複搜尋相同盤面時更新原紀錄，不重複增加步驟。
- 每顆實際補入的棋子都建立獨立步驟；回溯失敗的補子可顯示為未通過，但不得留在最終盤面。
- 每步保存 225 格盤面、225 格 N 點、攻方、步驟類型、標題、驗證狀態、理由與搜尋摘要。補子步驟另存棋色、座標、角色、階段及分支結果。
- 「最前」、「上一步」、「下一步」、「最後」操作同一索引；上一步／下一步每次只移動一格，第一步與最後一步停用無效按鈕。
- 切換步驟時顯示該步保存的盤面與 N 點，不沿用前一步圖層，也不受題目區「顯示 N 點」按鈕狀態影響。
- 成功時最後一步為通過的最終題目；失敗分支標示未通過；手動停止時，未完成步驟明確標示尚未完成驗證。
- 回放只接收核心具名事件，不得改變候選順序、棋盤、攻守方、搜尋或唯一性判斷。
- 核心事件至少包含產生開始／結束、初始材料、死四驗證開始／結束、搜尋結果／精簡結果、補子開始／結束及最終結果；巢狀事件以 operation id 與父操作關係連結。
- `makevcf-generator-progress.js` 不得覆寫 `genSetBusy`、`genValidateCandidate`、`genValidateExtensionCandidate`、`genShowResult`、`genEngine.findVCF` 或 `genEngine.trimGroups`，也不得透過另一套回放控制列採集畫面。

### 20.2 補子事件

- 中途補守由 `addLayerDefender()` 實際落子後送出事件。
- 最終唯一化由 `addFinalDefender()` 實際落子後送出事件。
- 補齊黑白子數由最終補色遞迴的實際候選分支送出事件。
- 中途補守與最終唯一化只能補入 `genOther(attacker)`；補齊子數可依差額補攻方或守方，事件棋色必須與補後盤面一致。
- 禁止攔截 `Worker.prototype.postMessage`、覆寫 `genEngine.findVCF()`、比較歷史盤面、操作回放按鈕或解析狀態文字來猜測補子事件、攻守方或階段；搜尋摘要只能來自核心直接送出的搜尋事件。
- 事件資料不一致時不得加入回放，並須在主控台報告錯誤。

### 20.3 狀態文字

狀態至少區分：

- 建立基礎。
- 驗證基礎。
- 增加與驗證死四。
- 計算與嘗試補守。
- 封鎖其他 VCF。
- 補齊黑白子數。
- 驗證最後補子。
- 重新建立、停止與完成。

### 20.4 結果資料

產生成功保存：

- 最終題目盤面、攻方、規則與題目步數。
- 目標完整手順、完成盤面與標準完成盤面。
- 每層 A、五點、方向、模板與永久新增棋子。
- N 點、自動補守棋、最終補齊棋。
- 抓禁手的禁點 A、禁手類型與模板資料。
- 最後一次成功驗證的節點數與組數。

`nodeCount` 是最後一次成功驗證搜尋的節點數，不是整次產生累計節點數。

「顯示答案」切換目標路線；「顯示 N 點」切換 N 點圖層。

### 20.5 執行內容與設定快照

- 每次按下產生時，先建立單一 `GenerationContext`，固定保存攻方、守方、規則、目標步數、設定快照與計數器。
- 產生期間的候選排序、補齊、唯一題與多組搜尋限制均讀取同一份設定快照，不得在搜尋途中重新讀取控制項改變政策。
- 題目產生器擴充設定以具名提供者註冊；一般控制項的忙碌狀態以具名 Hook 註冊，不得再覆寫 `genOptions` 或核心 `genSetBusy`。

## 21. GitHub Pages 部署

`.github/workflows/pages.yml` 負責：

- 建置棋型與 Bitboard WebAssembly。
- 執行 Native／Wasm 自我測試及 JavaScript 建置驗證。
- 以 `makevcf.html` 準備 `/index.html`。
- 上傳 Pages artifact。
- 部署 GitHub Pages。

要求：

- 建置腳本只能驗證或轉換目前正式程式，不能向正式檔案注入已淘汰的舊邏輯。
- Wasm 搜尋核心改變時必須使快取鍵失效或包含來源雜湊。
- `main` 推送後必須確認 `build` 與 `deploy` 都成功。
- GitHub Actions 產生的提交若不會再次觸發 workflow，必須以一般提交或 `workflow_dispatch` 觸發部署。

## 22. 驗收

### 22.1 搜尋核心

- Native C++ 編譯與自我測試。
- LTO SIMD Wasm 編譯。
- Wasm ABI 測試。
- JavaScript 語法檢查。
- `single`、`multi`、`shortest`、`shortest-one` 符合規格。
- `fast`、`strict` 的搜尋剪枝與輸出去重符合規格。
- 三種規則與 `0` 限制正確。
- 「活四不搜後續」只影響指定搜尋模式，且未勾選時不改變原路線集合。
- 限制中止時不誤報完整、最短或唯一。

### 22.2 題目產生器

- 所有 `makevcf-generator-*.js` 通過語法檢查。
- 使用獨立 Bitboard Worker，不影響主 Worker。
- 搜尋、防守與棋型請求明確傳入規則。
- 時間、節點、深度、64 組與剪枝設定正確。
- 1～10 步一般題與白方抓禁題可生成。
- 較短／其他 VCF 的勾選範圍正確。
- 搜尋達上限時依第 17.1 節處理。
- C++ 去重保留一手活四，並依選單切換集合子集／完全相同。
- 自動補守逐顆重搜、可回溯，且補守棋不會被後續模板移除。
- 最終補齊可依差額補攻方或守方，並重新驗證。
- 回放只有一條時間軸，上一步／下一步不跳號。
- 死四候選回傳前，其連五點已設為雙方 N，後續補子不會使用該點。
- 回放事件來自正式操作事件；補子來自實際補子分支，搜尋摘要來自核心搜尋事件，不使用 Worker 攔截、函式覆寫、歷史盤面或狀態文字推測。
- 桌機與手機操作正常。

### 22.3 文件

- `規格書.MD` 是唯一產品、搜尋及題目產生器規格。
- `AGENTS.md` 只保存開發與文件維護規則，不複製產品規格。
- `禁手判斷規格.MD` 只保存禁手演算法細節。
- 程式、介面、限制或資料格式變更時，同一批更新本文件。

## 21.4 題目產生器正式模組架構

- 核心流程固定分為：`core`、`base`、`layer`、`validate`、`main`、`search-policy`、`finalize`。
- `makevcf-generator-search-policy.js` 是較短 VCF、其他完成盤面、補守點排名與唯一目標盤面的唯一驗證政策。
- `makevcf-generator-finalize.js` 是最終唯一化、黑白子數補齊與補子後重新驗證的唯一流程。
- 題型、加成、設定與結果摘要集中於 `makevcf-generator-options.js`／`makevcf-generator-summary.js`，並透過具名 Registry 擴充，不得覆寫核心函式。
- `makevcf-generator-status-detail.js` 與 `makevcf-generator-progress.js` 都只能訂閱事件，不得改變搜尋或候選。
- 題目產生器與工作台 UI 依 `makevcf.html` 固定順序一次初始化；除 OpenCV 載入外，不使用全頁觀察器、定時輪詢或動態 script 注入。
