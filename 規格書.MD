# VCF 分析工具規格書

適用專案：`VCF`  
正式入口：`makevcf.html`、`/rapfi/`

> 本文件只描述正式產品規格。搜尋核心細節以《新版 Bitboard VCF 規格》為準；題目生成細節以《題目產生器規格》為準。

## 1. 產品目的

本工具提供 15×15 連珠／五子棋局面的 VCF 分析、題庫與題目產生功能，主要用途：

1. 手動建立或匯入棋盤。
2. 搜尋黑方或白方 VCF。
3. 顯示單組、多組及最短 VCF。
4. 尋找單一路線與整體 VCF 防守點。
5. 逐點分析連五、四與 VCF 候選點。
6. 補黑或補白後逐點找 VCF。
7. 顯示有禁規則下的黑棋禁手。
8. 使用 Supabase 題庫。
9. 產生指定 1～10 步的 VCF 題目。

本工具不是完整對局 AI；「VCT 點」是候選點分析，不代表已完成完整 VCT 數學證明。

## 2. 正式架構

### 2.1 `/rapfi/` Bitboard 工作台

```text
makevcf.html
  → rapfi/vcf-bitboard-main.js
  → rapfi/vcf-bitboard-worker.js
  → rapfi/engine/vcf-bitboard-engine.js / .wasm
```

### 2.2 題目產生器

```text
makevcf-generator-core.js
  → rapfi/vcf-bitboard-worker.js
  → rapfi/engine/vcf-bitboard-engine.js / .wasm
```

題目產生器使用自己的 Worker，不共用、中止或重新啟動主分析 Worker。

### 2.3 GitHub Pages

`.github/workflows/pages.yml` 負責：

- 建置棋型與 Bitboard WebAssembly。
- 準備靜態網站。
- 將 `makevcf.html` 作為首頁及 `/rapfi/` 工作台基礎。
- 注入題目產生器、工作台、最短 VCF、禁手顯示與題庫腳本。
- 發布 GitHub Pages。

## 3. 棋盤與資料模型

- 棋盤固定為 15×15，共 225 個交點。
- 索引：`idx = row × 15 + col`。
- `0 = 空點`、`1 = 黑棋`、`2 = 白棋`。
- 引擎棋盤可使用長度 226 的陣列，第 225 格為盤外相容值。
- VCF 路線是索引陣列，第一手為攻方，之後攻守交替。
- 主棋盤可保存於 `localStorage`，但題庫不得使用 `localStorage`。

## 4. 規則

| 值 | 名稱 | 說明 |
|---:|---|---|
| `0` | 自由 | 黑白長連皆算勝；黑白棋型皆取 9 格 |
| `1` | 無禁 | 黑白皆無禁手，長連不算勝也不算負；黑白棋型皆取 11 格 |
| `2` | 有禁 | 連珠規則；黑棋有禁手，黑棋取 11 格、白棋取 9 格 |

需求：

- 介面顯示「自由」、「無禁」、「有禁」。
- 搜尋期間不得切換規則。
- 主 Worker、Worker Pool、同步棋型函式與題目產生器必須使用相同規則值。
- 所有 Worker 請求都明確傳入規則，不能只依賴 Worker 內部狀態。
- 自由規則值為 `0`，不得被真假值回退誤判為有禁。

## 5. 棋盤操作

### FR-001 手動編輯

- 以 SVG 顯示 15×15 棋盤及星位。
- 點擊空點時依下一手顏色放置棋子。
- 點擊已有棋子的交點時移除該棋。
- 盤面變更後清除失效的 VCF、分析、題目答案及 N 點顯示。
- 盤面變更後重新計算禁手顯示。

### FR-002 清除

- 「清除路線／分析」只清除顯示圖層，不移除棋子。
- 「清空棋盤」移除全部棋子、路線、分析及題目狀態。

## 6. VCF 搜尋

### FR-101 單組 VCF

- 黑子 VCF、白子 VCF 使用 `single`。
- 找到第一組立即返回。
- 最大深度預設 200 ply。
- 可選擇是否精簡手順。
- 找到後依攻守順序顯示序號。

### FR-102 多組 VCF

- 使用 `multi`。
- 固定最多回傳 64 組。
- 可選擇高速或嚴格剪枝。
- 先以原始攻守手順做子集去重，再精簡路線。
- 提供上一組／下一組切換。
- 顯示唯一起點數、最短步數、組數、耗時及節點。

### FR-103 最短 VCF

- 使用 `shortest-one`。
- 先找到目前最佳路線，再排除所有更短路線。
- 完整排除更短路線時顯示已證明最短。
- 因限制中止時保留目前最佳路線，但必須顯示尚未證明最短。

### FR-104 搜尋限制

工作台提供：

- 時間限制，單位秒。
- 節點限制，單位百萬。
- `0` 代表不限制。

需求：

- 多組、最短、補子搜尋及題目產生器共用這兩個輸入。
- 多組型搜尋以 32-bit `maxNode` 封裝時間與節點限制。
- 不得以固定 500 萬節點取代使用者輸入。
- 達限制時可保留已找到結果，但必須標示搜尋未完整。
- 搜尋中停用會改變搜尋語意的控制項。

### FR-105 狀態與效能

完成提示格式：

```text
7.4351 秒，30,123,123 節點，4,123,123 節點/秒
```

- 秒數固定 4 位小數。
- 節點與每秒節點使用千分位整數。
- 中止、限制、尚未證明最短及列舉未完整等警告要與效能資訊一起顯示。

## 7. 防守與候選點分析

### FR-201 VCF 防守

- 「VCF 防守」針對目前顯示的 VCF 路線取得直接防守點。
- 沒有目前路線時按鈕停用或提示先搜尋 VCF。

### FR-202 全部 VCF 防守

- 先確認進攻方存在 VCF。
- 取得候選防守點。
- 逐點放入守方棋並重新搜尋。
- 放入後仍有 VCF 的點不是有效防守點。

### FR-203 VCT 點

- 先檢查盤面是否已有 VCF。
- 已有 VCF 時顯示路線。
- 無 VCF 時逐點顯示連五、四及 VCF 候選。
- 不得宣稱為完整 VCT 證明。

## 8. 補子搜尋

### FR-301 搜尋模式

下拉選單只保留：

- 「最速」：每個補子點使用 `single`。
- 「最短」：每個補子點使用 `shortest-one`。

### FR-302 顯示

- `5`：補子後直接連五。
- `4`：補子後形成四。
- 其他數字：補子後可形成的 VCF 步數。
- VCF 數字可依長短使用深淺色區分。
- 搜尋完成後，在所有 VCF 數字中以圓圈標示數字最大的落點。
- `4`、`5` 不參與圈選。
- 「最短」表示每個落點使用該點最短 VCF，不代表圈選全盤最小數字。

### FR-303 限制狀態

- 每個落點使用工作台目前時間與節點限制。
- 任一落點達限制時，整體提示必須標示搜尋未完整。
- 未完整時不得把目前未找到描述成已證明無 VCF。

## 9. 禁手顯示

### FR-401 顯示條件

- 只在有禁規則下顯示黑棋禁點。
- 白棋不顯示禁手。
- 自由與無禁規則不顯示禁手。

### FR-402 標記

- 長連：`6`。
- 四四：`44`。
- 三三：`33`。

### FR-403 圖層

- 使用獨立 SVG 圖層。
- 不得被 VCF 路線、補子標籤、防守三角形或一般分析標記清除。
- 每次落子、移除、清空、匯入棋盤或切換規則後重新計算。

## 10. 圖片匯入

### FR-501 輸入來源

- 檔案上傳。
- 桌機貼上截圖。
- 手機相機拍照。

### FR-502 流程

```text
載入圖片
  → 自動偵測棋盤四角
  → 使用者可拖曳修正
  → 透視校正
  → 建立 15×15 交點
  → 黑白空辨識
  → 預覽低信心點
  → 套用到主棋盤
```

### FR-503 限制

- 圖片處理在瀏覽器本機完成。
- 自動偵測失敗時提供手動四角框。
- 套用後清除舊題目答案、N 點及失效分析。

## 11. 題庫

### FR-601 資料來源

- 題庫唯一資料來源為 Supabase。
- 不得保留 `localStorage` 題庫、舊資料搬移或多後端相容層。

### FR-602 資料格式

每題保存：

- `board`：225 格棋盤。
- `attacker`：攻方。

載入題目後使用工作台目前選擇的規則。若未來要保存題目規則，必須同時修改資料表、讀寫程式與規格。

### FR-603 操作

- 上一題、下一題及題號定位。
- 新增目前盤面。
- 刪除目前題目。
- 載入後更新棋盤及攻方。

## 12. 題目產生器

### FR-701 基本設定

- 攻方：黑或白。
- 規則：自由、無禁、有禁。
- 生成步數：1～10。
- 沿用棋子加成。
- 棋子集中加成。
- 依加成高低排序。
- 補齊黑白子數。
- 三型加成。
- 只保留目標 VCF。

### FR-702 搜尋

- 一般候選使用 `shortest`。
- 只保留目標 VCF 使用 `multi`。
- 最大路線數 64。
- 最大深度為 `2 × 目標步數 + 1` ply。
- 共用工作台時間、節點及剪枝設定。
- 產生開始時取得設定快照，產生期間停用相關輸入。
- 規則值明確附帶於搜尋、精簡及防守請求。

### FR-703 唯一題

下列任一情況不得宣告唯一：

- `aborted = true`。
- 回傳 64 組。
- 達時間限制。
- 達節點限制。

### FR-704 加成

- 沿用棋子加成計算沿用攻方棋，以及 X 點或五點原有的守方棋；A 不計入。
- 棋子集中加成以盤面全部黑白棋的座標平均值為分布中心，模板中央點越接近中心分數越高。
- 未勾選排序時依權重隨機；勾選時依總權重由高到低，同權重隨機。

### FR-705 結果

- 題目套用到主棋盤。
- 可顯示／隱藏答案。
- 可顯示／隱藏 N 點。
- 摘要包含初始棋型、封鎖與補齊數量、沿用攻子、候選組數、黑白子數及最後一次多組 VCF 組數。
- `nodeCount` 是最後一次成功驗證的節點數，不是整次產生總節點數。

完整生成規則以 [`題目產生器規格.MD`](題目產生器規格.MD) 為準。

## 13. 行動裝置需求

- 棋盤寬度不得超出視窗。
- 按鈕與輸入可換行。
- 觸控落子、拍照、拖曳棋盤四角及題目產生器控制可正常操作。
- 搜尋中鎖定狀態在手機版必須可辨識。
- 禁手、VCF、分析及 N 點標記不可因縮放錯位。

## 14. 非功能需求

### NFR-001 本機運算

盤面搜尋與圖片處理在使用者裝置執行；題庫讀寫除外。

### NFR-002 不破壞主介面

題目產生器、題庫、禁手顯示及圖片匯入不得互相清除不相關狀態或中止不相關 Worker。

### NFR-003 可中止

長時間搜尋必須可停止；停止後相關 Worker 要重新就緒。

### NFR-004 文件一致

功能、名稱、限制、資料格式及執行路徑變更時，同一批更新相關 Markdown，不保留舊規格。

## 15. 驗收

### 15.1 搜尋核心

- Native C++ 編譯與自我測試通過。
- LTO SIMD Wasm 編譯通過。
- Wasm ABI 測試通過。
- JavaScript 語法檢查通過。
- `single`、`multi`、`shortest`、`shortest-one` 行為符合規格。
- `fast`、`strict` 剪枝符合規格。
- 自由、無禁、有禁三種規則正確。
- 時間、節點為 0 及非 0 時行為正確。
- 限制中止時不誤報完整、最短或唯一。

### 15.2 介面

- 桌機與手機棋盤、按鈕及控制項正常。
- 搜尋中不能切換規則或限制。
- 補子搜尋圈選最大 VCF 數字。
- 禁手圖層與其他分析圖層互不清除。
- 圖片匯入可套用並正確清除失效結果。

### 15.3 題目產生器與題庫

- 題目產生器使用獨立 Worker。
- 所有請求明確傳入規則。
- 時間、節點、深度、剪枝與 64 組上限正確傳入。
- 1～10 步生成流程正常。
- 唯一題達限制時不誤判。
- 題庫只使用 Supabase，新增、讀取、刪除一致。
- 相關 Markdown 與正式程式沒有殘留舊名稱、固定 500 萬、2～10 步、朝天元加成或沿用攻子等舊規格。
