# 使用者設定記憶規格

## 適用範圍

正式工作台與題目產生器中，由使用者手動調整的設定欄位都必須記住最後一次設定，重新整理或下次開啟頁面時自動恢復。

包含：

- 規則選擇。
- 分析色。
- 多組剪枝模式。
- 時間限制與節點限制。
- 補子搜尋模式。
- 「活四不搜後續」及其他搜尋勾選項。
- 題目產生器攻方、生成步數與加成數值。
- 題目產生器後續動態加入的排序、補子、唯一題及其他設定欄位。

## 儲存方式

- 使用瀏覽器 `localStorage`。
- 單選項以群組名稱保存選取值。
- 勾選項保存勾選狀態。
- 數字、文字、範圍及下拉選單保存目前值。
- 數字恢復時仍必須遵守欄位的 `min` 與 `max`。
- 已有各功能專用保存鍵時保持相容，不清除既有設定。

## 動態欄位

設定記憶層必須監聽以下容器內後續新增的控制項：

- `#rule-box`
- `#analysis-box`
- `#vcf-search-options`
- `#generator-panel`

新增功能只要把具有穩定 `id` 或 `name` 的設定欄位放入上述容器，就會自動納入記憶，不需要再重複撰寫個別保存程式。

## 不保存項目

以下屬於檔案、操作或一次性狀態，不視為人工設定：

- 檔案選擇欄位。
- 隱藏欄位。
- 按鈕與提交控制項。
- 棋盤內容、搜尋結果、答案顯示狀態及 N 點顯示狀態。
- 圖片匯入中的暫時校正資料。

## 恢復行為

- 頁面初始化完成後自動套用上次設定。
- 動態加入的欄位建立後也必須立即套用上次設定。
- 恢復會觸發正常的 `change` 流程，使規則與其他依賴設定同步更新。
- 同一網站在其他分頁修改設定時，可透過 `storage` 事件同步恢復。
