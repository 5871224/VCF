# 題目產生器回放規格

## 1. 單一回放面板

題目產生器只顯示一張回放卡片，不得另外建立「逐顆補子回放」或其他第二張回放卡片。

同一張卡片使用以下控制：

- 最前
- 上一步
- 目前步數／總步數
- 下一步
- 最後

## 2. 回放內容

單一時間軸依序包含：

1. 初始材料與禁手骨架。
2. 每層死四候選及驗證結果。
3. 中途封鎖較短或其他 VCF 時逐顆加入的守子。
4. 最終唯一化時逐顆加入的守子。
5. 補齊黑白子數時逐顆加入的攻方棋或守方棋。

逐顆補子紀錄必須保留「通過／未通過」、補子座標、實際補入角色及失敗回溯原因，不得因合併面板而省略。

## 3. 補子事件來源與攻守方

逐顆補子回放只能由實際補子分支直接送出事件：

- 中途補守由 `addLayerDefender()` 完成落子後送出。
- 最終唯一化由 `addFinalDefender()` 完成落子後送出。
- 補齊黑白子數由最終補色遞迴的實際候選分支送出。

每筆事件必須明確包含：

- 原題目的攻方。
- 實際補入顏色。
- 補入角色是攻方或守方。
- 補子座標。
- 補子後盤面。
- 補子階段。
- 分支最後通過或失敗。

中途補守與最終唯一化只能補入 `genOther(attacker)`。補齊黑白子數則可依盤面差額補攻方或守方，但補後盤面的指定座標必須等於事件所附的實際補入顏色。任一條件不符時不得加入回放，並須在主控台報告資料錯誤。

禁止以下推測式做法：

- 攔截 `Worker.prototype.postMessage` 猜測補子。
- 攔截所有 `genEngine.findVCF()` 搜尋猜測補子。
- 從歷史盤面集合猜父盤面或新增棋。
- 根據目前狀態文字猜攻方、守方或補子階段。

搜尋與補子邏輯本身不依賴回放；回放只能接收事件，不能改變候選順序、攻守方、棋盤或搜尋結果。

## 4. 補齊黑白子數的 VCF 驗證

補齊時，VCF 搜尋色永遠是原題目的攻方，不得改成這次補入的顏色。

補入守方棋時：

- 原攻方的目標 VCF 必須仍存在。
- 不得出現比目標更短的攻方 VCF。
- 「只保留目標 VCF」有啟用時，再沿用最終唯一化流程處理其他完成盤面。

補入攻方棋時：

- 原攻方的目標 VCF 必須仍存在。
- 不得出現比目標更短的攻方 VCF。
- 必須繼續搜尋同步數 VCF，不能因第一條最短路線仍是目標就提前接受。
- 若找到標準完成盤面不同的其他攻方 VCF，立即撤銷這顆補子並改試下一點。
- 同一目標完成盤面的不同手順不算新增 VCF，可以保留。

搜尋達目前限制但尚未找到其他 VCF 時，沿用專案既有規則：暫按「沒有其他 VCF」處理並保留搜尋受限標記。

## 5. 步數與操作

原完整回放有 `N` 步、逐顆補子有 `M` 步時，面板總步數顯示為 `N + M`。

例如完整回放為 `17 / 17`、補子紀錄為 `3 / 3`，合併後應顯示總計 `20` 步：

- 第 17 步按「下一步」進入第 18 步補子紀錄。
- 補子第一步按「上一步」返回第 17 步。
- 「最前」跳到整體第 1 步。
- 「最後」跳到整體最後一步。

沒有補子紀錄時，操作與原完整回放完全相同。

## 6. 介面限制

- 不得顯示兩組導航按鈕。
- 不得同時顯示兩個步數計數器。
- 舊的 `gen-stone-attempt-panel` 若存在，必須移除。
- 回放切換盤面時，逐顆補子紀錄不顯示前一個完整回放步驟的 N 點圖層。
- 產題進行中不得操作回放；產題結束或停止後才可瀏覽。
