# 題目產生器回放規格

## 1. 單一回放面板

題目產生器只顯示一張回放卡片，不得另外建立「逐顆補子回放」或其他第二張回放卡片。

同一張卡片使用以下控制：

- 最前
- 上一步
- 目前步數／總步數
- 下一步
- 最後

## 2. 回放內容

單一時間軸依序包含：

1. 初始材料與禁手骨架。
2. 每層死四候選及驗證結果。
3. 中途封鎖較短或其他 VCF 時逐顆加入的守子。
4. 最終唯一化時逐顆加入的守子。
5. 補齊黑白子數時逐顆加入的守子。

逐顆補子紀錄必須保留「通過／未通過」、補子座標及失敗回溯原因，不得因合併面板而省略。

## 3. 補子事件來源與攻守方

逐顆補子回放只能由實際補子分支直接送出事件：

- 中途補守由 `addLayerDefender()` 完成落子後送出。
- 最終唯一化由 `addFinalDefender()` 完成落子後送出。
- 補齊黑白子數由 `validateFilledState()` 的實際候選分支送出。

每筆事件必須明確包含：

- 攻方。
- 守方。
- 補子座標。
- 補子後盤面。
- 補子階段。
- 分支最後通過或失敗。

守方必須等於 `genOther(attacker)`，且補子後盤面的指定座標必須確實是守方棋。任一條件不符時不得加入回放，並須在主控台報告資料錯誤。

禁止以下推測式做法：

- 攔截 `Worker.prototype.postMessage` 猜測補子。
- 攔截所有 `genEngine.findVCF()` 搜尋猜測補子。
- 從歷史盤面集合猜父盤面或新增棋。
- 根據目前狀態文字猜攻方、守方或補子階段。

搜尋與補守邏輯本身不依賴回放；回放只能接收事件，不能改變候選順序、攻守方、棋盤或搜尋結果。

## 4. 步數與操作

原完整回放有 `N` 步、逐顆補子有 `M` 步時，面板總步數顯示為 `N + M`。

例如完整回放為 `17 / 17`、補子紀錄為 `3 / 3`，合併後應顯示總計 `20` 步：

- 第 17 步按「下一步」進入第 18 步補子紀錄。
- 補子第一步按「上一步」返回第 17 步。
- 「最前」跳到整體第 1 步。
- 「最後」跳到整體最後一步。

沒有補子紀錄時，操作與原完整回放完全相同。

## 5. 介面限制

- 不得顯示兩組導航按鈕。
- 不得同時顯示兩個步數計數器。
- 舊的 `gen-stone-attempt-panel` 若存在，必須移除。
- 回放切換盤面時，逐顆補子紀錄不顯示前一個完整回放步驟的 N 點圖層。
- 產題進行中不得操作回放；產題結束或停止後才可瀏覽。
