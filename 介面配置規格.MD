# VCF 介面配置規格

> 本文件只描述目前正式介面，不保留舊版配置與修改歷程。

## 1. 頁面標題與區塊標題

- 頁面主標題為「VCF 分析與題目工具」。
- 頁面主標題下方不顯示說明文字。
- 棋盤、搜尋與分析、圖片匯入等主要區塊只顯示標題，不在標題下方顯示說明段落。
- 功能執行中的必要狀態、錯誤、提示與圖片辨識說明仍保留在各自的狀態區域，不視為標題說明。

## 2. 規則控制

- 規則使用單一下拉選單。
- 選項順序為「有禁」、「無禁」、「自由」。
- 規則值分別為 `2`、`1`、`0`。
- 既有 `input[name="rules"]` 保留為隱藏相容狀態，供搜尋、題目產生器與既有程式讀取。
- 下拉選單切換時必須同步隱藏 radio、主 Worker、Worker Pool、同步棋型函式與題目產生器規則。
- 搜尋或題目產生期間，下拉選單必須停用。

## 3. 搜尋與分析區塊

- 原「基本搜尋」與「進階分析」整合為單一「搜尋與分析」區塊。
- 規則、分析色、單組 VCF、多組 VCF、最短 VCF、防守、VCT 選點與補子搜尋都位於同一卡片內。
- 原有控制項 ID 與按鈕 ID 必須保留，避免影響既有事件、搜尋流程與後載入擴充。
- 多組 VCF 的「同型-活四取前一手」選項仍位於多組 VCF 控制附近，功能不因卡片整合而改變。

## 4. 響應式配置

- 桌機維持棋盤與搜尋控制左右配置。
- 窄螢幕改為單欄排列。
- 搜尋與分析卡片在所有寬度都只佔一欄，不得因舊有雙卡片媒體規則縮成半寬。
