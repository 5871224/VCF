# VCF 介面規格

## 主要版面

- 棋盤固定顯示在左側。
- 棋盤右側使用三個分頁：
  - `VCF計算`
  - `VCF 題目產生器`
  - `圖片匯入`
- 手機或窄螢幕時，棋盤與分頁區改為上下排列。
- 分頁切換只隱藏或顯示既有功能區，不重建棋盤、Worker、搜尋狀態或控制項事件。
- 頁面初始化時先等待必要控制項建立，再一次顯示完整分頁介面，避免控制項逐個出現或載入後重新跳位。
- 所有原有控制項 ID、事件、搜尋功能與使用者設定必須保留。

## VCF計算分頁

### 分析

同一行放置：

- 分析色。
- 顯示禁手。

### VCF 搜尋

同一行放置：

- 速找 VCF。
- 最短 VCF。
- 計算設定勾選。

「速找 VCF」依目前分析色決定搜尋黑方或白方，沿用單組 VCF 找到第一組立即返回的流程。原本黑方與白方單組按鈕保留為隱藏相容入口。

### 計算設定

計算設定預設以卡片呈現，只有勾選「計算設定」時顯示。卡片包含：

- 時間限制。
- 節點限制。
- 精簡手順。
- 規則下拉選單。

規則下拉選單依序為「有禁」、「無禁」、「自由」，背景仍保留 `input[name="rules"]` 相容狀態。搜尋期間必須停用。

### 多組 VCF

同一行放置：

- 多組 VCF。
- 上一組。
- 下一組。
- 多組設定勾選。

### 多組設定

多組設定預設以卡片呈現，只有勾選「多組設定」時顯示。卡片包含：

- 多組剪枝下拉選單。
- 活四不搜後續。
- 同型-活四取前一手。

多組剪枝顯示文字簡化為：

- `集合子集` → `pruning = fast`
- `完全同盤` → `pruning = strict`

只修改顯示文字，不改變既有值、搜尋模式或儲存鍵。

### 防守

同一行放置：

- 單一路線防守。
- 全部路線防守。

### 延伸搜尋

同一行放置：

- VCT 選點。
- 補黑找 VCF。
- 補白找 VCF。
- 補子搜尋下拉選單。

### 棋盤操作

同一行放置：

- 停止。
- 繼續搜尋。
- 清除標記。
- 清空棋盤。

上述按鈕只搬移位置與調整顯示名稱，不得更換原有控制項 ID 或事件處理流程。

## VCF 題目產生器分頁

- 題目條件與候選偏好分成兩個清楚區塊。
- 產生、停止、顯示答案及顯示 N 點按鈕維持同一操作列。
- 狀態、回放、驗證結果、N 點圖例與說明文字不得刪除。
- 介面調整不得改變題目產生、停止、回放、補子紀錄或驗證流程。

## 圖片匯入分頁

- 匯入、拍照、重新偵測、確認、返回、套用及重設按鈕使用整齊的操作列。
- 辨識狀態、原圖／預覽畫布、手動修正與說明文字不得刪除。
- 介面調整不得改變圖片載入、棋盤框調整、辨識、手動修正或套用流程。

## 初始化與相容

- `#vcf-search-options`、最短 VCF、顯示禁手、活四不搜後續及同型設定可能由不同功能腳本建立；正式介面必須在初始顯示前等待它們就緒。
- 初始排版完成後，不得在整個 `document.body` 保留監看所有子節點的永久 `MutationObserver`，避免題目回放、圖片預覽或狀態更新觸發反覆重排。
- 個別控制項若初始化較慢，只能在初始顯示前使用有次數或時間上限的等待流程；完成排版後不得持續掃描及搬動全部控制項。
- 設定勾選狀態與控制列順序使用明確 class 更新，不使用需要在大型分頁 DOM 上反覆重新比對的動態 `:has()` 選擇器。
- `#vcf-search-options` 與隱藏的規則 radio 必須留在文件中供既有搜尋、題目產生器、禁手顯示、設定保存及 Worker 相容使用。
