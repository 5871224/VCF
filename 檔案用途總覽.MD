# VCF 專案檔案用途總覽

本文件說明版本庫內所有 Git 追蹤檔案的用途與執行狀態；目前共 130 個檔案。產品行為仍以 [`規格書.MD`](規格書.MD) 為唯一正式規格；本文件回答「檔案由誰使用、屬於哪一條路徑、是否為正式執行檔」，並集中記錄正式程式的架構、載入順序與跨模組相依性，供接手維護的人員或 AI 快速建立正確心智模型。

## AI 接手快速導覽

### 先讀哪些文件

接手本專案時，建議依下列順序讀取：

1. `AGENTS.md`：先確認不可跨越的開發邊界、文件與測試規則。
2. `規格書.MD`：確認目前產品應該怎麼運作；這是唯一產品行為規格。
3. `檔案用途總覽.MD`：確認實際檔案分工、載入順序與相依性；不要只靠檔名猜用途。
4. `禁手判斷規格.MD`：只有修改棋型、長連、三三／四四禁手或遞迴證明時才需要深入閱讀。
5. 若修改 Rapfi／Wasm 來源，再讀 `rapfi/SOURCE.md`。

### 正式網站與執行主鏈

正式網站只有：

```text
https://5871224.github.io/VCF/
```

版本庫與 Pages 的正式入口都是根目錄 `index.html`。核心主鏈如下：

```text
index.html
  ├─ 頁面 DOM、SVG 棋盤、基本工作台控制項
  ├─ 圖片匯入／棋盤框選／透視校正／黑白子辨識的基礎 runtime
  ├─ rapfi/vcf-bitboard-main.js
  │    ├─ rapfi/engine/vcf-bitboard-engine.js + .wasm
  │    └─ rapfi/vcf-bitboard-worker.js
  └─ 頁尾依固定順序載入正式功能模組
       ├─ rapfi/vcf-bitboard-generator-compat.js
       ├─ makevcf-generator-*.js
       ├─ rapfi/rapfi-bitboard-dashboard.js
       ├─ rapfi/vcf-shortest-vcf-ui.js
       ├─ rapfi/vcf-forbidden-overlay.js
       ├─ rapfi/rapfi-workbench-header.js
       ├─ rapfi/vcf-record-tools.js
       ├─ rapfi/rapfi-question-bank.js
       └─ makevcf-generator-image-import-fix.js 等介面模組
```

`index.html` 是唯一正式工作台 HTML 來源；不要另外建立第二套入口，也不要直接修改 Pages 產物來修功能。

### 主要狀態的擁有者

| 狀態／服務 | 正式擁有者 | 主要使用者 | 維護注意事項 |
|---|---|---|---|
| 15×15 主棋盤 | Rapfi `Board`／`VCFWorkbenchRecord` 是唯一盤面來源；`index.html` SVG 僅 renderer | 搜尋、題目產生器、禁手、棋譜、圖片匯入 | `_renderBoardArr`／相容 `_setBoardArr` 只能由 Rapfi record 層重畫；功能模組一律用 `playAt`、`replacePosition`、`replaceHistory`、`clearPosition`。 |
| 規則 | `rapfi/vcf-bitboard-generator-compat.js` 的 `vcfSetRules` 契約 | Worker、搜尋、題目產生器、禁手 | `0=自由、1=無禁、2=有禁`；不可用會把 `0` 吃掉的真假值 fallback。 |
| 搜尋 RPC／Worker Pool | `rapfi/vcf-bitboard-main.js` | dashboard、最短 VCF、題目產生器相容層 | Wasm ABI、Worker request 格式與規則值必須同步；此熱路徑不得依賴 `VCFRapfiDB`／棋譜 DB。 |
| 工作台生命週期／Registry | `rapfi/vcf-bitboard-generator-compat.js` | dashboard、最短、題庫、產生器 UI、狀態與 busy hook | 不得由後載模組覆寫 `setBusy`、`setStatus`、搜尋函式或引擎方法。 |
| 工作模式 | `rapfi/rapfi-workbench-header.js` 的 `VCFWorkbenchRecord.workspace()` | 主棋盤、匯入／匯出、題庫、圖片、題目產生器 | `record` 打譜模式從空盤記錄合法交替 history 並使用 YXDB；`puzzle` 題目模式先自由擺盤、明確指定攻方，再以 `basePly` 為根建立解答分支並使用 `vcf-puzzle-v1`。編輯／瀏覽模式只是操作偏好，不是工作模式。 |
| 棋譜 position DB、目前 `Board` history、position `recordText`／後續選點 | `rapfi/vcf-rapfi-db-bridge.cpp`（Rapfi `Board`／`DBClient`／`DBRecord`）＋ `rapfi/vcf-rapfi-db.js` | `rapfi-workbench-header.js`、`vcf-record-tools.js` | Pages 由固定 `RAPFI_REF` 編譯輕量 `VCFRapfiDBModule`，不載入 Rapfi 搜尋網路。bridge ready 後 canonical position、history、注釋、`@BTXT@`、child、PASS child、分支刪除與規則切換都以 Rapfi C++ 為正式且唯一 backend；snapshot 與 restore 都直接序列化或解析 Rapfi `YXDBStorage` records，避開 Emscripten 虛擬檔案串流越界。單次 UI notify 對同一局面只查一次 children。此模組不得參與 `findVCF`／Bitboard Worker 搜尋。標記可位於棋子上這項既有能力仍保留。 |
| 目前路徑／工作台持久化 | Rapfi `Board.history` ＋ `rapfi-workbench-header.js` 的模式序列化 | 匯入、導覽、YXDB／RenLib／VCF 題目匯出 | 打譜模式的 localStorage 保存 YXDB snapshot、history 與 rule；題目擺盤階段保存可回復的本機草稿，進入解答階段後改存 `vcf-puzzle-v1` 的初始盤面、攻方、目前相對路線與 position `recordText`／children，不把 setup 假手順當成正式棋譜。兩者都不保存第二份 renderer 盤面；Rapfi 仍是執行期唯一可操作 backend。 |
| 圖片辨識暫存盤面 | `index.html` 圖片匯入 IIFE 的 `importState.recognizedBoard` | 辨識預覽、人工修正、套用主棋盤 | 這只是辨識局面，不等於棋譜 history。 |
| 圖片手順標記 | `makevcf-generator-image-import-fix.js` 的 `orderByIndex` | 圖片辨識後人工補手順、`VCFWorkbenchRecord.replaceHistory()` | 與黑白辨識分離；只有完整連續且黑奇白偶才可轉 exact history。 |
| 題目產生狀態 | `makevcf-generator-core.js` 的 `GenerationContext` 與 Registry | base／layer／validate／search-policy／finalize／progress | 一次生成共用同一 context；不要讓 UI 在生成中重新讀控制項改政策。 |

### 關鍵載入順序與硬相依

頁尾正式腳本的相對順序有語意，不應任意調換：

```text
makevcf-mobile.js
emoji/emoji.js
EvaluatorJScript.js → EvaluatorCore.js → Evaluator.js
rapfi/vcf-bitboard-generator-compat.js
makevcf-generator-core.js
  → base.js
  → layer.js
  → validate.js
  → integrated.js
  → options.js
  → main.js
  → summary.js
  → search-policy.js
  → finalize.js
  → progress.js
  → status-detail.js
rapfi/rapfi-bitboard-dashboard.js
rapfi/vcf-shortest-vcf-ui.js
rapfi/vcf-forbidden-overlay.js
makevcf-generator-open-four-stop.js
makevcf-layout.js
rapfi/engine/vcf-rapfi-db.js → rapfi/vcf-rapfi-db.js
rapfi/rapfi-workbench-header.js
rapfi/vcf-record-tools.js
rapfi/rapfi-question-bank.js
makevcf-generator-settings-persistence.js
makevcf-generator-image-import-fix.js
makevcf-optimized-search-v2.js
```

其中幾個硬相依特別重要：

- `vcf-bitboard-generator-compat.js` 必須早於依賴 Registry／事件的 dashboard、題目產生器及其他工作台模組。
- Rapfi Board 資料層與 Bitboard 計算層互不依賴：`rapfi-workbench-header.js` 先建立 `VCFWorkbenchRecord` API，根工作台再用 idle callback 依序 lazy load `rapfi/engine/vcf-rapfi-db.js` 與 `rapfi/vcf-rapfi-db.js`，由 `vcf-rapfi-db-ready` 啟用棋盤。不能再以 Bitboard `ready`／`failed` 決定 Rapfi 是否啟動。
- dashboard、最短 VCF、禁手標示與活四設定先建立計算控制項，`makevcf-layout.js` 再一次掛入六個正式區塊，是唯一工作台版面擁有者；`rapfi-question-bank.js` 建立題庫後送出 `vcf-question-bank-ready`，由它把題庫放到產生器最下方，不要改回後載補丁、輪詢或全頁 DOM 觀察。
- `makevcf-generator-image-import-fix.js` 需要 `index.html` 已存在的 `#source-canvas`、`#btn-import-apply` 等 DOM，也需要後載完成的 `VCFWorkbenchRecord` 才能把合法手順轉成正式 history。
- `makevcf-optimized-search-v2.js` 現在只是相容載入檔，不是另一套正式搜尋主流程。

### 圖片匯入 → 工作模式 → 對應格式的資料流

「確認棋盤區域」後不是固定先辨識棋子，而是由 `index.html` 顯示模式選擇，分成兩條正式資料流：

```text
圖片
  ↓
index.html
  ├─ 四角／透視校正
  └─ 確認棋盤區域
       ↓
       模式選擇視窗
       ├─「辨識棋子」
       │    ↓
       │  recognizedBoard[225]      // 0空、1黑、2白
       │    ↓ 人工修正
       │  （可選）加上手順
       │    ↓
       │  orderByIndex[225]         // 只能標在已辨識棋子
       │    ↓ 固定棋色 + 黑奇白偶驗證
       │
       └─「加上手順」記譜紙模式
            ↓ 不做 recognizedBoard 棋色辨識
          orderByIndex[225]         // 可標任意交點
            ↓
          odd => BLACK / even => WHITE
            ↓
          由手數反建 board[225]

兩路匯合
  ├─ 只有靜態局面
  │    ↓
  │  題目模式 + VCFWorkbenchRecord.replacePosition(board, attacker)
  │    ↓
  │  Rapfi setup position + basePly
  │    ↓
  │  vcf-puzzle-v1（初始盤面、攻方、注釋、標記、解答分支）
  │
  └─ 有完整可信 1..N 手順
       ↓
     history = [{ index, stone }, ...]
       ↓
     打譜模式 + VCFWorkbenchRecord.replaceHistory(history)

Rapfi Board / DB 成為唯一正式棋盤
  ↓
導覽／鏡像／旋轉／canonical 注釋與盤面標記
  ├─ 打譜模式 → YXDB／RenLib
  └─ 題目模式 → VCF 題目容器
```

圖片手順目前的重要實作契約：

- `index.html` 擁有圖片幾何、透視與 `recognizedBoard`；按「確認棋盤區域」只完成透視，不得立即強制辨識棋子，之後由模式選擇視窗決定走辨識或記譜紙手順。
- 辨識棋子模式沿用 `recognizedBoard` 的固定棋色；進入手順模式時以辨識盤面作為標號驗證基準，不把 renderer 當資料來源。這條路只能在已辨識黑／白棋子上標號。
- 記譜紙模式由 `VCFImageMoveOrder.startNotationPaperMode()` 進入，不呼叫黑白子辨識，也不要求交點有棋子；`orderByIndex` 是唯一輸入，奇數手建立黑棋、偶數手建立白棋。
- 記譜紙模式的 `board` 是衍生資料，不是圖片辨識結果；插入 1、插入 2、整理缺號或重新編號後都必須重新由手數奇偶產生棋色，不可沿用舊顏色判斷錯位。
- 手順模式在 `sourceCanvas` 的 `pointerup` capture 階段攔截事件；辨識模式避免同一次點擊又觸發「空→黑→白→空」，記譜紙模式則允許任何 15×15 交點成為手順位置。已標交點的單次左鍵只能提示，不得覆蓋；覆蓋必須走 `dblclick`，且雙擊空交點仍只建立一次一般標記。
- `makevcf-generator-image-import-fix.js` 同時擁有 `orderByIndex` 與手順編輯 undo/redo stack；標記、雙擊覆蓋、清除、插入 1／2、compact 都必須透過同一 mutation/history 路徑，執行新編輯後清除 redo 分支。
- `makevcf-mobile.js` 只負責右側窄手順表、數字 DOM overlay、數字棋游標，以及桌機右鍵／觸控長按清除的呈現與事件橋接；觸控長按完成後要在 capture 階段吃掉該次 `pointerup`，避免核心手順編輯器把剛清除的點重新標記。不得保存第二份手順歷史，也不得再實作「整理缺號」預覽／確認狀態。
- 「整理缺號」現在直接改正式 `orderByIndex` 並形成一筆 undo 記錄；使用者若不接受結果，直接按「回上一步」，不再維護套用／復原按鈕或 presentation-only snapshot。
- 辨識模式 exact 條件：每顆已辨識棋子都有且只有一個連續 `1..N` 手數，且固定棋色符合黑奇白偶。
- 記譜紙模式 exact 條件：目前所有已標記交點的手數恰為連續 `1..N`；棋色由 N 的奇偶直接生成，因此不存在固定棋色 parity mismatch。
- 完整手順由 `VCFWorkbenchRecord.replaceHistory()` 重播驗證；沒有可信手順的辨識結果只能走 `replacePosition()`，不得用座標順序猜 history。
- YXDB setup path 只讀 `VCFWorkbenchRecord.snapshot()` 的 history/exact；不得用座標排序、辨識順序或畫面位置偽造 history。
- 圖片重新載入、重新偵測、返回棋盤框或重設匯入會清掉手順狀態；辨識模式黑白子改動後重新進入手順時，也要移除已變成空點的舊編號。

### 修改時的影響範圍

| 要改的功能 | 至少一起檢查 |
|---|---|
| 圖片框選／透視／黑白辨識 | `index.html`、`makevcf-generator-image-import-fix.js`、圖片相關規格與 `tests/workbench-architecture.test.js` |
| 圖片手順 | `index.html` 的模式分流、`makevcf-generator-image-import-fix.js` 的兩種 source mode／undo-redo／覆蓋政策、`makevcf-mobile.js` 呈現與右鍵橋接、`rapfi/rapfi-workbench-header.js` 的 `VCFWorkbenchRecord` 契約、`vcf-record-tools.js` 手順顯示、YXDB 匯出 |
| 棋譜導覽／history／分支 | `rapfi/rapfi-workbench-header.js`、`rapfi/vcf-record-tools.js`、YXDB／RenLib 匯入匯出 |
| 主棋盤／規則事件 | `index.html`、`vcf-bitboard-generator-compat.js`、`vcf-bitboard-main.js`、Worker、禁手與題庫 |
| 搜尋 ABI／限制 | C++ Bitboard source、Wasm build、`vcf-bitboard-worker.js`、`vcf-bitboard-main.js`、dashboard、題目產生器 runtime tests |
| 題目產生政策 | `core/base/layer/validate/main/search-policy/finalize` 相關模組與 generator CI；不要在 UI 模組平行實作第二套政策 |
| Pages 公開檔案或載入順序 | `index.html`、`tools/prepare-pages-site.py`、`.github/workflows/pages.yml`、source-order／architecture tests |

狀態分類：

- **正式**：GitHub Pages 正式工作台或題目產生器會載入。
- **建置**：只在編譯、部署或來源同步時使用。
- **測試**：只供自我測試或比較驗證。
- **桌面**：Electron、WebView2、Native 或本機 Web UI。
- **實驗室**：只供 `/rapfi/lab.html` 或研究用途。
- **相容**：保留給舊 Evaluator、本機入口或 fallback，不是正式 Bitboard 主路徑。
- **參考**：背景、專題或子系統說明，不是另一份正式產品規格。
- **上游**：外部專案的未修改快照，不是正式執行來源。
- **待清理**：目前沒有正式載入或呼叫者，刪除前仍須完成引用與回歸確認。

`.git/`、`node_modules/`、CMake 輸出、`.cache/`、`_site/` 及 Pages 建置產生的 `rapfi/engine/*` 不屬於 Git 追蹤來源，因此不逐檔列入。

## 1. 根目錄與文件

| 檔案 | 狀態 | 用途 |
|---|---|---|
| `.gitignore` | 建置 | 排除本機編譯、快取、套件與暫存輸出。 |
| `package.json` | 建置／測試 | 不含第三方依賴的統一測試與來源驗證命令入口。 |
| `AGENTS.md` | 建置 | 專案開發邊界、文件維護、驗證與部署規則。 |
| `README.md` | 正式 | 專案入口、正式架構與文件導覽。 |
| `規格書.MD` | 正式 | 工作台、搜尋、題庫與題目產生器的唯一產品規格。 |
| `禁手判斷規格.MD` | 正式 | 棋型、長連與禁手遞迴的專門實作規格。 |
| `檔案用途總覽.MD` | 建置 | 本文件；維護全版本庫檔案用途、正式載入關係與跨模組相依性。 |
| `五子棋.md` | 參考 | 五子棋／連珠棋型、禁手與 VCF 的背景及演算法說明。 |
| `index.html` | 正式 | 唯一根工作台 HTML 與 Pages 入口，包含 SVG 棋盤、圖片匯入基礎 runtime、`importState.recognizedBoard` 與舊引擎 fallback。 |

## 2. 題目產生器與工作台 JavaScript

| 檔案 | 狀態 | 用途 |
|---|---|---|
| `makevcf-generator-core.js` | 正式 | 常數、棋盤工具、專用 Worker RPC、GenerationContext、事件與所有具名 Registry。 |
| `makevcf-generator-base.js` | 正式 | 初始活三／死四材料模板、放置枚舉與固定排序政策。 |
| `makevcf-generator-layer.js` | 正式 | 新死四模板、修補變體、延伸候選與候選裝飾入口。 |
| `makevcf-generator-validate.js` | 正式 | VCF 路線重播、標準完成盤面、層記錄與驗證共用函式。 |
| `makevcf-generator-integrated.js` | 正式 | 將控制項、結果與 N 點繪製整合到主棋盤。 |
| `makevcf-generator-options.js` | 正式 | 沿用／集中加成、排序、補齊、唯一題等設定提供者與結果摘要。 |
| `makevcf-generator-main.js` | 正式 | 單一產生主流程、材料提供者選擇、逐層延伸及最終化呼叫。 |
| `makevcf-generator-summary.js` | 正式 | 白方抓禁題型、禁手骨架、候選與完成盤面裝飾。 |
| `makevcf-generator-search-policy.js` | 正式 | 唯一的較短／其他 VCF 分類、補守排名、遞迴驗證與最終目標盤清理政策。 |
| `makevcf-generator-finalize.js` | 正式 | 唯一的最終黑白子補齊、補子後重新驗證與產生結果完成流程。 |
| `makevcf-generator-progress.js` | 正式 | 訂閱核心事件，建立單一回放時間軸、逐顆補子、N 點與導航。 |
| `makevcf-generator-status-detail.js` | 正式 | 註冊狀態格式器並訂閱核心事件顯示各階段狀態。 |
| `makevcf-generator-open-four-stop.js` | 正式 | 活四不搜後續與搜尋限制封裝；使用正式請求提供者及 busy hook。 |
| `makevcf-layout.js` | 正式 | 工作台唯一版面擁有者；在計算控制項建立後一次掛入六個區塊、三分頁、產生器操作列與題庫，並把 `.db`／`.lib`／`.vcf.json` 分流到對應匯入 API，不輪詢 DOM。 |
| `makevcf-generator-settings-persistence.js` | 正式 | 固定載入後一次綁定並保存工作台與產生器設定。 |
| `makevcf-generator-image-import-fix.js` | 正式 | 數字棋子中心清理、Hough 線／缺邊晶格修正，以及圖片手順資料核心。擁有 `orderByIndex`、辨識棋子／記譜紙兩種 source mode、單擊覆蓋保護、雙擊覆蓋、undo/redo、跳號、插入 1／2、compact、奇偶棋色推導與合法 history bridge；只在等待 OpenCV 時允許有上限的短期重試。 |
| `makevcf-mobile.js` | 正式 | 手機棋盤自適應，以及圖片手順的呈現層：棋盤右側窄表、DOM 手數標記、數字棋游標與右鍵清除事件橋接。不得擁有第二份 `orderByIndex`、undo/redo stack 或 compact 確認狀態。 |
| `makevcf-optimized-search-v2.js` | 相容 | 僅保留檔名供既有 HTML 載入順序；舊漸進搜尋與主流程覆寫已移除。 |

## 3. GitHub Actions

| 檔案 | 狀態 | 用途 |
|---|---|---|
| `.github/workflows/pages.yml` | 建置 | 編譯 Native／Wasm、執行架構、實際 Worker 與 Rapfi DB snapshot 回歸測試，只組裝根工作台入口並部署 GitHub Pages。 |
| `.github/workflows/pages-status.yml` | 建置 | 把 Pages workflow 成功或失敗狀態回報到版本庫。 |
| `.github/workflows/vcf-generator-ci.yml` | 測試 | 題目產生器語法、架構、專用 Worker、規則、限制、剪枝與回放的唯一完整 CI。 |
| `.github/workflows/vcf-native-ci.yml` | 測試 | Native C++、LTO SIMD Wasm、自我測試與 ABI 驗證。 |
| `.github/workflows/workbench-ci.yml` | 測試 | 工作台 JavaScript、生命週期 Registry、檔案清理與部署架構驗證。 |
| `.github/workflows/build-renju-evaluator.yml` | 建置 | 手動以 Emscripten 重建上游 Renju Evaluator。 |
| `.github/workflows/vendor-renju-evaluator.yml` | 建置 | 從上游儲存庫匯入未修改 Evaluator 快照並提交。 |

## 4. `rapfi/` 正式 Bitboard 與實驗室

### 4.1 頁面與 JavaScript

| 檔案 | 狀態 | 用途 |
|---|---|---|
| `rapfi/index.html` | 實驗室 | Rapfi／棋型比較來源頁；Pages 部署時只改名為 `/rapfi/lab.html`，不建立 `/rapfi/` 入口。 |
| `rapfi/SOURCE.md` | 參考 | Rapfi、棋型 Wasm、Bitboard Wasm 的來源與隔離說明。 |
| `rapfi/rapfi-app.js` | 實驗室 | Rapfi 指令介面、棋盤與比較頁主控制器。 |
| `rapfi/rapfi-vcf-tools.js` | 實驗室 | 實驗室候選點與 VCF 棋型比較工具。 |
| `rapfi/rapfi-worker.js` | 實驗室 | 載入官方 Rapfi 與獨立棋型 Wasm 的 Worker。 |
| `rapfi/vcf-candidate-worker.js` | 實驗室 | 實驗室候選點棋型分析 Worker。 |
| `rapfi/vcf-bitboard-main.js` | 正式 | 主執行緒 Bitboard Wasm、RPC Worker、Worker Pool 與舊介面相容服務；Worker init 與主執行緒 Wasm init 各有 15 秒 timeout，失敗只回報計算引擎，不影響 Rapfi 棋盤。 |
| `rapfi/vcf-bitboard-worker.js` | 正式 | VCF 搜尋、精簡、防守、補點與棋型 RPC Worker。 |
| `rapfi/vcf-bitboard-generator-compat.js` | 正式 | 工作台生命週期 runtime：統一規則、Rapfi board-change 事件、狀態格式、搜尋 dispatcher 與題目產生器同步棋型相容 API；不再包裝 renderer mutation 或從 click 猜盤面變更。 |
| `rapfi/rapfi-bitboard-dashboard.js` | 正式 | 單組、多組、補子搜尋與工作台控制列；透過正式 Registry 註冊，不覆寫核心函式。 |
| `rapfi/vcf-shortest-vcf-ui.js` | 正式 | 最短 VCF、活四同型選項與最短證明狀態；使用 trim／status／busy Registry。 |
| `rapfi/vcf-forbidden-overlay.js` | 正式 | 黑棋禁手 Worker 與盤面標記圖層；訂閱盤面及規則事件。 |
| `rapfi/rapfi-workbench-header.js` | 正式 | 精簡頁首、打譜／題目工作模式與唯一 `VCFWorkbenchRecord`／主棋盤資料 API；手動落子、setup position、完整 history、清空、導覽、position／注釋／`@BTXT@`／children 全由 Rapfi 管理。打譜模式匯入／匯出 YXDB，題目模式驗證並匯入／匯出 `vcf-puzzle-v1`，且提供損壞本機狀態復原；VCF 計算結果不寫入兩種儲存格式。 |
| `rapfi/vcf-lz4-cloud.js` | PHP 主機版 | Google 雲端 YXDB 儲存／載入；儲存時直接使用 `VCFWorkbenchRecord.exportYXDB()` 的 Rapfi snapshot，不維護或讀取第二套 JavaScript 棋譜樹。GitHub Pages 無 PHP API，不載入此模組。 |
| `rapfi/vcf-record-tools.js` | 正式 | 15×15 棋譜工具列；讀取 `VCFWorkbenchRecord` 提供瀏覽／編輯模式、盤面標記、刪除後續分支、PASS、手順、鏡像／旋轉、標題／解說顯示與 PNG 截圖；目前版本設定遷移預設開啟編輯模式。 |
| `rapfi/record-svg/double_arrow_left.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：前一分支／起始點。 |
| `rapfi/record-svg/arrow_left.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：前一手。 |
| `rapfi/record-svg/arrow_right.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：次一手。 |
| `rapfi/record-svg/double_arrow_right.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：次一分支／末端。 |
| `rapfi/record-svg/photo.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：截圖。 |
| `rapfi/record-svg/edit.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：編輯模式。 |
| `rapfi/record-svg/font.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：標記模式。 |
| `rapfi/record-svg/Aa.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：A 標記快捷鍵。 |
| `rapfi/record-svg/star.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：星號標記快捷鍵。 |
| `rapfi/record-svg/arrow.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：箭頭標記快捷鍵。 |
| `rapfi/record-svg/delete.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：清空標記輸入。 |
| `rapfi/record-svg/cancel.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：刪除目前棋子及後續分支。 |
| `rapfi/record-svg/number.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：手順顯示。 |
| `rapfi/record-svg/settings.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：棋譜設定。 |
| `rapfi/record-svg/dock_top.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：標題欄顯示。 |
| `rapfi/record-svg/dock_left.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：解說欄顯示。 |
| `rapfi/record-svg/flip.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：鏡像盤面。 |
| `rapfi/record-svg/rotate_90.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：旋轉 90 度。 |
| `rapfi/record-svg/forbidden.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：禁手顯示。 |
| `rapfi/record-svg/circle.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：手順減少值。 |
| `rapfi/record-svg/circle_n.svg` | 正式 | 從原五子棋打譜頁沿用的 SVG：隱藏前 N 手手順。 |
| `rapfi/rapfi-question-bank.js` | 正式 | Supabase 題庫讀寫及題目載入；訂閱正式盤面事件與 busy hook。API 請求 10 秒 timeout，載入失敗顯示重新載入按鈕，不得無限 loading。 |

### 4.2 C++／Wasm 正式來源

| 檔案 | 狀態 | 用途 |
|---|---|---|
| `rapfi/vcf-pattern-engine.cpp` | 參考 | 獨立棋型引擎基準來源。 |
| `rapfi/vcf-pattern-engine-optimized.cpp` | 正式 | 14 種棋型、長連與禁手的正式 Native／Wasm 實作。 |
| `rapfi/vcf-bitboard-engine.cpp` | 正式 | Bitboard ABI、棋盤轉換、統計與搜尋共用入口。 |
| `rapfi/vcf-bitboard-legacy-extra.cpp` | 正式 | 題目產生器需要的舊棋型函式相容匯出。 |
| `rapfi/vcf-bitboard-search-v2.cpp` | 正式 | 組合搜尋分片並提供正式單組、多組、最短與掃點匯出。 |
| `rapfi/vcf-bitboard-search-single-tt-v5.inc` | 正式 | 單組搜尋 256K 四路精確同型表。 |
| `rapfi/vcf-bitboard-search-fast-part1.inc` | 正式 | 搜尋資料結構、棋盤與增量視窗基礎。 |
| `rapfi/vcf-bitboard-search-fast-part2.inc` | 正式 | 候選、棋型與搜尋輔助邏輯。 |
| `rapfi/vcf-bitboard-search-fast-part3.inc` | 正式 | 單組 DFS 與主要搜尋熱路徑。 |
| `rapfi/vcf-bitboard-search-fast-part4.inc` | 正式／對照 | 舊 V3 實作，以重新命名方式納入同一翻譯單元供相容及比較。 |
| `rapfi/vcf-bitboard-search-exact-tt-v3.inc` | 正式 | 多組搜尋精確同盤表。 |
| `rapfi/vcf-bitboard-search-time-limit-v4.inc` | 正式 | 多組搜尋時間與節點限制 context。 |
| `rapfi/vcf-bitboard-search-shortest-one-v1.inc` | 正式 | 單執行緒最短單組搜尋與證明狀態。 |
| `rapfi/vcf-bitboard-search-multi-v3.inc` | 正式 | 多組、fast／strict、去重及掃點搜尋。 |
| `rapfi/vcf-bitboard-search-first-nontarget-v1.inc` | 正式 | 題目產生器第一條較短／其他 VCF 模式；由正式 V3 ABI 呼叫。 |

Pages 產生的 `rapfi/engine/rapfi-single-simd128.*`、`vcf-pattern-engine.*`、`vcf-bitboard-engine.*` 是建置產物，不在版本庫內。

## 5. `eval/` 舊 Evaluator 相容路徑

| 檔案 | 狀態 | 用途 |
|---|---|---|
| `eval/EvaluatorCore.js` | 相容 | 上游 Evaluator 高階棋型、VCF 與雜湊核心的本地執行副本。 |
| `eval/Evaluator.js` | 相容 | 載入 `EvaluatorCore.js` 並套用本地 `excludeBlockVCF` 修正。 |
| `eval/EvaluatorJScript.js` | 相容 | 純 JavaScript 棋型與搜尋 backend。 |
| `eval/EvaluatorWebassembly.js` | 相容 | 舊 `Evaluator.wasm` 的 JavaScript 包裝器。 |
| `eval/Evaluator.wasm` | 相容 | 舊 Evaluator WebAssembly 二進位。 |
| `eval/worker.js` | 相容 | `index.html` 本機 fallback 與舊搜尋比較使用的 Worker 指令層。 |
| `emoji/emoji.js` | 相容 | 舊 Evaluator 使用的棋型符號與常數。 |

## 6. `bitboard/` 早期對照原型

| 檔案 | 狀態 | 用途 |
|---|---|---|
| `bitboard/BitBoard256.h` | 測試 | C++ 16×16／256-bit 座標與落子原型。 |
| `bitboard/bitboard.js` | 測試 | JavaScript `BigUint64Array` 對照實作。 |
| `bitboard/tests/bitboard_test.cpp` | 測試 | C++ 座標及落子測試。 |
| `bitboard/tests/bitboard.test.mjs` | 測試 | JavaScript 座標及落子測試。 |
| `bitboard/README.md` | 參考 | 原型 API 與測試命令；其中提及的 `bitboard/index.html` 目前不存在。 |

## 7. 測試與建置工具

| 檔案 | 狀態 | 用途 |
|---|---|---|
| `tests/generator-layout-replay.test.js` | 測試 | 題目產生器版面、核心事件回放、逐顆補子、N 點與單一步進導航測試。 |
| `tests/generator-source-order.test.js` | 測試 | 驗證根頁固定腳本順序、無分散式動態載入及建置腳本不改寫來源。 |
| `tests/generator-context.test.js` | 測試 | 驗證設定提供者、忙碌 Hook、核心事件／巢狀 operation、設定快照凍結與 GenerationContext 生命週期。 |
| `tests/generator-architecture-v2.test.js` | 測試 | 驗證正式模組順序、刪除舊版政策檔、禁止全域函式覆寫與 DOM 輪詢。 |
| `tests/generator-registry-v2.test.js` | 測試 | 驗證狀態、搜尋請求、候選、層記錄、分析、材料及結果 Registry 的優先順序與組合。 |
| `tests/generator-search-policy-v2.test.js` | 測試 | 驗證較短 VCF 會以正式補守政策封鎖並保護補守 N 點。 |
| `tests/generator-finalize-v2.test.js` | 測試 | 驗證已平衡盤面可由單一 finalizer 直接完成。 |
| `tools/prepare-pages-site.py` | 建置 | 以明確瀏覽器資源 allowlist 組裝 Pages artifact；不把需要 PHP API 的雲端棋譜／Google 登入腳本注入 GitHub Pages 入口。 |
| `tools/prepare-server-site.py` | 建置 | 在已建置的靜態 artifact 加入 Google Identity、雲端 YXDB 與查盤 Wasm，並注入明確 API URL；僅用於 `/BD1/` 伺服器版，不改寫 Pages 來源。 |
| `tests/prepare-server-site.test.py` | 測試 | 驗證 BD1 伺服器 artifact 的 API 設定、腳本順序、YXDB engine 與部署 revision metadata。 |
| `tests/workbench-architecture.test.js` | 測試 | 驗證無桌面版／死檔、正式工作台 Registry、規則 0、Rapfi YXDB／RenLib 匯出、圖片 adapter 與 Pages allowlist。 |
| `tests/runtime-performance-contract.test.js` | 測試 | 驗證正式環境只做 ABI 冒煙檢查、Pool 不重複自測、規則廣播合併、fallback 驗證重用與熱路徑不建立多餘盤面副本。 |
| `tests/bitboard-worker-runtime.test.js` | 測試 | 使用實際 Wasm 驗證規則 0、快速／fallback 防點及 Worker 啟動契約。 |
| `tests/generator-worker-runtime.test.js` | 測試 | 驗證題目產生器使用獨立 Bitboard Worker、規則與搜尋限制傳遞、剪枝選單、busy 狀態及防點回傳正規化。 |

## 8. `upstream/lfz084-renju/` 未修改快照

| 檔案 | 狀態 | 用途 |
|---|---|---|
| `upstream/lfz084-renju/UPSTREAM.md` | 上游 | 上游位置、commit、日期與授權注意事項。 |
| `upstream/lfz084-renju/SOURCE_MANIFEST.txt` | 上游 | 匯入來源及快照檔案清單。 |
| `upstream/lfz084-renju/README.upstream.md` | 上游 | 上游專案原始 README。 |
| `upstream/lfz084-renju/BUILDING.md` | 上游／建置 | 本專案隔離重建 Evaluator 的步驟。 |
| `upstream/lfz084-renju/build-evaluator.sh` | 上游／建置 | Linux／Emscripten 建置腳本。 |
| `upstream/lfz084-renju/build-with-docker.ps1` | 上游／建置 | Windows Docker 建置入口。 |
| `upstream/lfz084-renju/eval/Evaluator.cpp` | 上游 | 上游 Evaluator C++ 原始碼。 |
| `upstream/lfz084-renju/eval/Evaluator.html` | 上游 | 上游示範／操作頁。 |
| `upstream/lfz084-renju/eval/Evaluator.js` | 上游 | 上游高階棋型與搜尋核心。 |
| `upstream/lfz084-renju/eval/EvaluatorJScript.js` | 上游 | 上游純 JavaScript backend。 |
| `upstream/lfz084-renju/eval/EvaluatorWebassembly.js` | 上游 | 上游 Wasm 包裝器。 |
| `upstream/lfz084-renju/eval/Evaluator.wasm` | 上游 | 上游編譯後 Wasm。 |
| `upstream/lfz084-renju/eval/Evaluator.wat` | 上游 | 上游 Wasm text 格式，用於檢查與研究。 |
| `upstream/lfz084-renju/eval/engine.js` | 上游 | 上游 Worker Pool 管理器。 |
| `upstream/lfz084-renju/eval/worker.js` | 上游 | 上游 Worker 指令層。 |

上游快照只供重建、相容測試與差異比較；正式執行檔來自根目錄 `eval/` 或 `rapfi/`，不得直接在快照內套用本地修正。

## 9. 維護規則

- 新增、刪除、重新命名或改變檔案職責時，同一批變更更新本文件。
- 正式載入路徑改變時，同步更新 `規格書.MD`、Pages workflow 及本文件狀態。
- 生成檔與快取不列入本文件；若生成物改為 Git 追蹤，必須補列用途。
- 專案只維護瀏覽器版；`app/` 與 `cpp/` 已完整移除。
- 工作台功能模組使用具名 Registry／事件，不得重新覆寫正式全域函式。
- Pages 以明確 allowlist 組裝公開檔案，不部署 C++ 原始碼、測試原型或未使用相容檔。
- 修改跨模組功能時，先從本文件的「主要狀態擁有者」「硬相依」與「修改時的影響範圍」確認責任邊界，再依 `規格書.MD` 更新產品行為，避免用後載 patch 覆蓋正式來源。

## 題庫掛載契約補充

- `rapfi/rapfi-question-bank.js`：建立 Supabase 題庫介面並發出 `vcf-question-bank-ready`，讓版面模組以明確事件接手掛載。
- `makevcf-layout.js`：收到題庫 ready event 後，將題庫固定移到 `#generator-panel` 最下方；題庫尚未建立時不使用輪詢或延遲補丁。
- `tests/generator-architecture-v2.test.js`：驗證題庫 ready event、立即／後備初始化及產生器最下方掛載契約，防止載入順序再次造成位置回退。

## 執行期效能覆核補充

- `rapfi/vcf-bitboard-worker.js`：正式啟動只做固定盤面搜尋冒煙檢查；完整自測留在 CI。防點 fallback 快取第一次成功驗證的統計與結果，避免同點重搜。
- `rapfi/vcf-bitboard-main.js`：合併相同規則廣播、Pool 略過重複冒煙檢查，並以直接 heap 寫入取代短命 `Uint8Array`。
- `rapfi/vcf-candidate-worker.js`：直接讀取 Wasm 結果緩衝區，不為每個候選建立兩層切片。
- `makevcf-generator-validate.js`：活四標準盤面只在確定需要時複製；移除未被正式政策呼叫的舊驗證入口。
