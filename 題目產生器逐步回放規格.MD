# VCF 題目產生器逐步回放規格

> 本文件描述題目產生器正式使用的驗證回放功能。

## 1. 顯示時機

- 題目產生期間不再即時刷新棋盤。
- 每次按下「產生 VCF」時清除上一輪回放紀錄。
- 產生成功、失敗或手動停止後，才顯示本輪回放面板。
- 回放面板不得改變題目產生、搜尋、補守或唯一性判斷結果。

## 2. 紀錄範圍

每輪產生必須依實際驗證順序保存：

1. 每一個送入驗證的死四候選盤面。
2. 自動封鎖較短 VCF 時，每補上一顆守方棋後的盤面。
3. 「只保留目標 VCF」逐顆補守後的盤面。
4. 補齊黑白子數時，每補上一顆棋後的盤面。
5. 最終成功題目盤面。

相同盤面在同一驗證分支重複搜尋時更新原紀錄，不重複增加回放步驟；不同死四候選即使盤面相同，仍視為不同驗證步驟。

## 3. 每步資料

每個回放步驟至少保存：

- 225 格黑白棋盤面。
- 攻方顏色。
- 步驟類型：死四、補守、封鎖其他 VCF、補齊子數或最終題目。
- 步驟標題；補子步驟須顯示棋色與座標。
- 驗證狀態：通過、未通過、驗證中或紀錄。
- 驗證理由與搜尋摘要。

死四步驟可顯示該層的 A、五點及模板編號。

## 4. 驗證理由

可由搜尋與手順重播明確判定時，未通過理由必須顯示具體原因，例如：

- 未找到 VCF。
- 搜尋達限制且未找到可驗證結果。
- 搜尋路線無法重播為合法 VCF。
- 仍存在比目標短的 VCF。
- 最短 VCF 步數不是目標步數。
- 步數正確但標準完成盤面不符。
- 此補子分支未保留目標 VCF或未被採用。

通過時須說明目標步數與標準完成盤面已符合預期；若死四加入補守後才通過，也須明確標示。

## 5. 回放操作

- 面板提供「上一步」與「下一步」。
- 顯示目前序號與總步數。
- 第一筆時停用「上一步」，最後一筆時停用「下一步」。
- 切換步驟時，主棋盤顯示該步保存的盤面。
- 面板同時顯示通過／未通過標章、步驟標題與理由。
- 手機版按鈕與文字不得超出畫面。

## 6. 結束狀態

- 產生成功時，最後一筆為「最終題目」，狀態為通過。
- 產生失敗時，未完成的分支標為未通過。
- 手動停止時，尚未完成的步驟標示「產生已停止，這一步未完成驗證」。
- 顯示回放時不得自動播放或持續刷新棋盤。

## 7. 驗收

1. `makevcf-generator-progress.js` 通過 JavaScript 語法檢查。
2. 產生期間棋盤不再隨驗證進度即時切換。
3. 成功後可從第一步逐步切換到最終盤面。
4. 每個死四候選都有獨立紀錄。
5. 每次逐顆補子後都有盤面紀錄。
6. 每步均顯示驗證狀態；未通過時顯示原因。
7. 新一輪產生會清除上一輪紀錄。
8. 手機版可操作。
