# VCF 題目產生器逐步回放規格

> 本文件描述題目產生器正式使用的驗證回放功能。

## 1. 顯示時機

- 題目產生期間不再即時刷新棋盤。
- 每次按下「產生 VCF」時清除上一輪回放紀錄。
- 產生成功、失敗或手動停止後，才顯示本輪回放面板。
- 回放面板不得改變題目產生、搜尋、補守或唯一性判斷結果。

## 2. 紀錄範圍

每輪產生必須依實際建立與驗證順序保存：

1. 每次選中的初始活三盤面。
2. 每次選中的初始死四盤面。
3. 白方抓禁手題建立的禁手骨架盤面，須在白棋死四候選之前顯示，並標示長連、四四或三三及禁點 A。
4. 每一個送入驗證的死四候選盤面。
5. 自動封鎖較短 VCF 時，每補上一顆守方棋後的盤面。
6. 「只保留目標 VCF」逐顆補守後的盤面。
7. 補齊黑白子數時，每補上一顆棋後的盤面。
8. 最終成功題目盤面。

初始材料即使沒有產生可驗證的死四候選，也必須保留在回放中。相同盤面在同一驗證分支重複搜尋時更新原紀錄，不重複增加搜尋步驟；不同的初始材料選取或死四候選即使盤面相同，仍視為不同建立步驟。

## 3. 每步資料

每個回放步驟至少保存：

- 225 格黑白棋盤面。
- 225 格 N 點遮罩；每格分別保存黑方不可用、白方不可用或雙方不可用。
- 攻方顏色。
- 步驟類型：初始活三、初始死四、禁手骨架、死四候選、補守、封鎖其他 VCF、補齊子數或最終題目。
- 步驟標題；補子步驟須顯示棋色與座標。
- 驗證狀態：通過、未通過、驗證中或紀錄。
- 驗證理由與搜尋摘要。

初始活三與死四可顯示材料名稱及模板文字。禁手骨架須顯示禁手種類與禁點 A。死四候選可顯示該層的 A、五點及模板編號。同一死四候選後續補守與重新驗證的盤面沿用該候選的 N 點遮罩；最終題目使用成功結果的完整 N 點遮罩，包含答案路線加入的雙方 N 點。

## 4. 驗證理由

可由搜尋與手順重播明確判定時，未通過理由必須顯示具體原因，例如：

- 未找到 VCF。
- 搜尋達限制且未找到可驗證結果。
- 搜尋路線無法重播為合法 VCF。
- 仍存在比目標短的 VCF。
- 最短 VCF 步數不是目標步數。
- 步數正確但標準完成盤面不符。
- 此補子分支未保留目標 VCF 或未被採用。

初始材料與禁手骨架尚未進入 VCF 驗證時，狀態顯示為「紀錄」，並說明下一步將建立死四候選。通過時須說明目標步數與標準完成盤面已符合預期；若死四加入補守後才通過，也須明確標示。

## 5. 回放操作

- 面板提供「最前」、「上一步」、「下一步」與「最後」。
- 「最前」直接切換到第一筆初始盤面。
- 「最後」直接切換到最後一筆盤面。
- 顯示目前序號與總步數。
- 第一筆時停用「最前」與「上一步」。
- 最後一筆時停用「下一步」與「最後」。
- 切換步驟時，主棋盤顯示該步保存的盤面及 N 點，不受題目區「顯示 N 點」按鈕狀態影響。
- 雙方 N 點使用綠色方塊；黑方 N 點使用黑底紅框方塊；白方 N 點使用白底紅框方塊。
- 面板同時顯示通過／未通過／紀錄標章、步驟標題與理由。
- 手機版按鈕與文字不得超出畫面；按鈕可自動換行。

## 6. 結束狀態

- 產生成功時，最後一筆為「最終題目」，狀態為通過。
- 產生失敗時，未完成的分支標為未通過。
- 手動停止時，尚未完成的步驟標示「產生已停止，這一步未完成驗證」。
- 顯示回放時不得自動播放或持續刷新棋盤。

## 7. 實作檔案

- `makevcf-generator-progress.js`：保存原有死四驗證、補子與最終盤面回放。
- `makevcf-generator-replay-complete.js`：保存初始活三、初始死四、禁手骨架與每個候選的 N 點遮罩，按實際順序合併完整回放、繪製三種 N 點並提供最前／最後操作。
- `makevcf-generator-reuse-bonus.js`：在原回放器載入後載入完整回放擴充。

## 8. 驗收

1. `makevcf-generator-progress.js`、`makevcf-generator-replay-complete.js` 與載入器均通過 JavaScript 語法檢查。
2. 題目產生期間棋盤不再隨驗證進度即時切換。
3. 成功後可從最初盤面逐步切換到最終盤面。
4. 「最前」與「最後」可直接跳至第一筆及最後一筆。
5. 每次選中的初始活三、初始死四都有紀錄。
6. 抓禁手題在白棋死四候選前顯示長連、四四或三三禁手骨架。
7. 每個死四候選都有獨立紀錄。
8. 每次逐顆補子後都有盤面紀錄。
9. 每一步都顯示黑方、白方與雙方 N 點，且樣式與題目區圖例一致。
10. 最終題目回放使用成功結果的完整 N 點遮罩。
11. 每步均顯示驗證狀態；未通過時顯示原因。
12. 新一輪產生會清除上一輪紀錄。
13. 手機版可操作。
