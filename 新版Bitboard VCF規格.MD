# 新版 Bitboard VCF 規格

> 本文件定義 `/rapfi/` 唯一正式 Bitboard 工作台、搜尋模式、規則、限制、禁手顯示及題目產生器整合。只保留目前正式規格。

對外正式網址為 `https://5871224.github.io/VCF/`；根網址、`index.html` 與 `makevcf.html` 都必須轉往 `/VCF/rapfi/`，不得維護另一套舊版工作台。

## 1. 正式執行路徑

```text
/VCF/、/VCF/index.html、/VCF/makevcf.html
  → 轉址 /VCF/rapfi/
  → makevcf.html（工作台建置來源）
  → rapfi/vcf-bitboard-main.js
  → rapfi/vcf-bitboard-worker.js
  → rapfi/engine/vcf-bitboard-engine.js / .wasm
```

入口規則：

- `/rapfi/index.html` 是唯一正式介面。
- 根頁與 `makevcf.html` 只保留舊網址相容轉址，不得各自載入或維護另一套介面功能。
- 所有正式 UI、題目產生器、搜尋選項及錯誤修正只修改 `/rapfi/` 使用的工作台來源。

主要檔案：

| 檔案 | 用途 |
|---|---|
| `makevcf.html` | `/rapfi/` 的 15×15 SVG 棋盤、共用按鈕、狀態列及圖片匯入建置來源；不是獨立正式入口 |
| `rapfi/vcf-bitboard-main.js` | 同步 Wasm、主 Worker、Worker Pool 及頁面服務 |
| `rapfi/vcf-bitboard-worker.js` | 獨立 Worker RPC 與 Bitboard Wasm 呼叫 |
| `rapfi/rapfi-bitboard-dashboard.js` | 搜尋選項、工作台流程與補子搜尋 |
| `vcf-live-four-grouping-option.js` | 工作台活四同型比較選項，以及題目產生器固定保留活四手的相容層 |
| `rapfi/vcf-shortest-vcf-ui.js` | 「最短 VCF」按鈕與最短證明狀態 |
| `rapfi/vcf-forbidden-overlay.js` | 黑棋禁手標示圖層 |
| `rapfi/vcf-bitboard-generator-compat.js` | 題目產生器同步棋型、禁手及規則相容層 |
| `rapfi/rapfi-workbench-header.js` | 精簡頁首、Rapfi 實驗室連結及重新整理 |
| `rapfi/rapfi-question-bank.js` | Supabase 題庫 |
| `rapfi/vcf-pattern-engine-optimized.cpp` | 棋型、長連與禁手核心 |
| `rapfi/vcf-bitboard-search-v2.cpp` | VCF 搜尋核心 |

## 2. 棋盤與規則

棋盤固定為 15×15，共 225 格：

- `0`：空點。
- `1`：黑棋。
- `2`：白棋。
- 傳入引擎時可使用第 226 格作盤外相容值。

規則值固定為：

| 值 | 介面名稱 | 長連 | 禁手 | 棋型範圍 |
|---:|---|---|---|---|
| `0` | 自由 | 黑白長連皆算勝 | 無 | 黑白皆取 9 格 |
| `1` | 無禁 | 黑白長連不算勝也不算負 | 無 | 黑白皆取 11 格 |
| `2` | 有禁 | 黑棋依連珠判定，白棋長連算勝 | 黑棋檢查長連、四四、三三 | 黑棋取 11 格、白棋取 9 格 |

不得以真假值回退處理規則；自由規則 `0` 必須完整保留。每個 Worker 請求都要明確傳入規則值。

## 3. 搜尋模式

### 3.1 `single`

用途：最快找到一組 VCF。

- 找到第一組立即返回。
- 使用專用 256K、四路精確同型表。
- 不列舉全部路線。
- 預設最大深度為 200 ply。
- `pruning` 選項不改變單組流程。

### 3.2 `multi`

用途：在指定最大深度內列舉多組 VCF。

- 固定最多回傳 64 組。
- 直接使用指定 `maxDepth`，不逐層加深。
- 固定執行路線精簡。
- 先以原始攻守手順做子集去重，再精簡保留下來的路線。
- 達路線、時間或節點限制時保留已找到結果，並回傳中止狀態。

### 3.3 `shortest`

用途：取得最短層的一批 VCF。

- 只搜尋合法的奇數 ply 深度。
- 逐層加深，第一個找到結果的深度就是目前最短層。
- 回傳該最短層內保留下來的多組路線。
- 固定精簡。
- 使用與 `multi` 相同的 `fast`／`strict` 剪枝選擇。
- 因限制中止時不得宣告已完整列出最短層。

### 3.4 `shortest-one`

用途：只回傳一組已證明最短或目前最佳的 VCF。

- 全程使用單一 Worker、單執行緒，不拆分根候選。
- 先在最大深度內找到一組路線作為目前最佳上限。
- 合法 VCF 路線長度為奇數 ply；排除更短路線時使用 `bestPly - 2`。
- 找到更短反例後更新最佳路線，再以新的 `bestPly - 2` 繼續。
- 各次縮限共用完整黑白盤面的精確無解集合。
- 無解集合最多保存 524,288 個盤面；滿後停止新增，不覆蓋既有證明。
- 只有所有較短範圍完整無解時，才設定 `shortestProven = true`。
- 因時間或節點限制中止時，保留目前最佳路線，但介面必須顯示「尚未證明為最短」。

## 4. 多組剪枝

### 4.1 `strict`

- 只在完整黑棋集合與完整白棋集合都相同時判定同型。
- 不以棋子子集代替完整同盤。
- 適合需要最保守、最容易比對的列舉。

### 4.2 `fast`

- 先使用完整同盤無解剪枝。
- 只有已完整證明的勝型才可加入集合子集剪枝。
- 未完整搜尋、因限制中止或尚未證明的狀態不得寫入勝型子集表。

### 4.3 路線後處理

正式順序：

```text
原始路線
  → 以攻守落子集合做子集去重
  → 保留代表路線
  → 依「同型-活四取前一手」建立同型 key
  → 再次去重
  → 依步數與穩定順序顯示
```

「同型-活四取前一手」只影響工作台多組 VCF 的第二次同型比較：

- 預設勾選。
- 勾選：最後一手形成活四時，同型 key 不包含該活四手，等同取到活四前一手再比較。
- 未勾選：同型 key 包含該活四手，必須連活四落點也相同才視為同型。
- 無論是否勾選，畫面保留並顯示代表路線的原始完整手順，不直接截短顯示路線。

不得先精簡所有路線再做原始手順子集判斷。

## 5. 時間與節點限制

工作台提供：

- 時間限制：秒。
- 節點限制：百萬節點。
- `0` 代表該項不限制。

多組型搜尋以 32-bit `maxNode` 封裝：

```text
bit 31      ：封裝標記
bit 0..9    ：節點上限，單位百萬；0 = 不限制
bit 10..30  ：時間上限，單位秒；0 = 不限制
```

未設封裝標記時，數值直接作為節點上限。

規則：

- 多組 VCF、最短 VCF、補子搜尋及題目產生器共用同一組時間／節點輸入。
- 題目產生器不得固定傳入 5,000,000；必須使用產生開始時取得的輸入快照。
- 時間每 524,288 節點檢查一次。
- 達限制時保留已找到結果，但狀態列必須說明搜尋未完整。
- 限制中止時，不能把「目前最短」、「目前未找到」或「沒有其他路線」描述為完整證明。

## 6. 工作台功能

### 6.1 基本 VCF

- 黑子 VCF、白子 VCF 使用 `single`。
- 可選擇是否精簡手順。
- 找到後以棋盤序號顯示完整攻守手順。

### 6.2 多組 VCF

- 使用 `multi`。
- 固定最多 64 組。
- 提供「同型-活四取前一手」勾選項，預設勾選並保存使用者設定。
- 勾選項只控制多組路線後處理的同型 key，不改變搜尋核心、VCF 合法性或原始手順。
- 搜尋期間停用此勾選項。
- 顯示唯一起點數、最短步數、原始與同型整理後組數、耗時及節點。
- 提供上一組／下一組切換。

### 6.3 最短 VCF

- 使用 `shortest-one`。
- 共用多組搜尋的時間與節點限制。
- 完整證明時顯示最短；限制中止時只顯示目前最佳及尚未證明。

### 6.4 VCF 防守

- 「VCF 防守」針對目前顯示路線取得直接防守點。
- 單一路線防守必須使用產生目前手順時記錄的攻方顏色，不得改用當下「分析色」；白方手順若以黑方身分重播會造成全盤空點誤判。
- 計算防點前必須先在原始盤面驗證指定路線確實成立；原路線無效或驗證中止時回傳空集合，不得進入全盤逐點防守判定。
- 「全部 VCF 防守」先取得候選防守點，再逐點放入守方棋，排除放入後仍有 VCF 的位置。
- 固定路線驗證在攻擊後只剩一個連五點時，必須先檢查守方落在該點是否合法；有禁規則下黑棋若為長連、四四或三三禁手，該路線仍視為白棋獲勝，不要求手順包含非法防守手。
- 抓禁路線只有在預補守方棋後使唯一防點變為合法，且完整路線不再成立時，該預補點才算防點；無關點不得因手順在攻擊手結束而被誤判為防點。
- 長連禁手無法透過額外補黑解禁。

### 6.5 VCT 點

- 先檢查盤面是否已有 VCF。
- 已有 VCF 時直接顯示路線。
- 沒有 VCF 時逐點掃描連五、四及可形成 VCF 的候選點。
- 此功能不是完整 VCT 數學證明。

### 6.6 補子搜尋

下拉選單只保留：

- 「最速」：每個落點使用 `single`。
- 「最短」：每個落點使用 `shortest-one`。

規則：

- 補黑、補白均使用分析色作為 VCF 進攻方。
- 每個落點分別套用目前時間／節點限制。
- `4`、`5` 分別表示形成四與直接連五，不視為 VCF 步數。
- 所有 VCF 數字以顏色深淺表示長短。
- 搜尋完成後，在所有顯示 VCF 步數的落點中，圓圈標示數字最大的落點。
- 「最短」只代表每個落點顯示該點的最短 VCF，不代表圈選全盤最小數字。
- 任一落點因限制中止時，結果提示必須標示搜尋未完整。

## 7. 棋型與禁手

- 棋型與禁手只計算原落點四方向一次。
- 正五、長連及四四直接依該結果判定。
- 只有疑似三三才複製完整棋盤並驗證活四延伸點。
- 公開禁手函式不得修改傳入盤面。
- 白棋及自由／無禁規則不執行黑棋禁手限制。

「顯示禁手」：

- 只在有禁規則下顯示黑棋禁點。
- 長連顯示 `6`。
- 四四顯示 `44`。
- 三三顯示 `33`。
- 使用獨立 SVG 圖層，不得被 VCF 路線或一般分析標記清除。
- 每次落子、移除棋子、清空、匯入棋盤或切換規則後重新計算。

## 8. 題目產生器整合

題目產生器使用獨立 Bitboard Worker：

```text
makevcf-generator-core.js
  → rapfi/vcf-bitboard-worker.js
  → rapfi/engine/vcf-bitboard-engine.js / .wasm
```

正式規則：

- 取消或重新啟動題目產生器時，只處理自己的 Worker。
- 一般候選使用 `shortest`。
- 「只保留目標 VCF」使用 `multi`。
- 兩種模式都依多組剪枝下拉選單使用 `fast` 或 `strict`。
- 最大深度為 `2 × 目標步數 + 1` ply。
- `maxVCF = 64`。
- `maxNode` 使用工作台時間／節點限制的封裝值，不使用固定 500 萬。
- 產生開始時取得限制及剪枝快照；產生期間停用這些控制項。
- 規則 `0`、`1`、`2` 必須明確傳入搜尋、精簡及防守請求。
- 題目產生器比較相同 VCF 時，一律把最後活四手保留在同型 key；不讀取工作台「同型-活四取前一手」勾選項。
- 唯一題只要 `aborted=true`、回傳 64 組、達時間限制或達節點限制，就不得宣告唯一。

題目產生器完整規格以 [`題目產生器規格.MD`](題目產生器規格.MD) 為準。

## 9. 題庫

- 題庫唯一資料來源為 Supabase。
- 題庫保存 225 格 `board` 與 `attacker`。
- 載入題目後使用工作台目前選擇的規則。
- 不保留瀏覽器 `localStorage` 題庫、搬移流程或其他儲存相容層。

## 10. Worker RPC

主執行緒送出：

```js
{ id, type, data }
```

Worker 回傳：

```js
{ id, ok, result, error }
```

常用 `type`：

- `init`
- `setGameRules`
- `findVCF`
- `findShortestVCF`
- `trimVCFGroups`
- `getBlockVCF`
- `getLevelPoints`
- `cancel`

每個與棋盤規則相關的請求都必須在 `data.rules` 明確傳入 `0`、`1` 或 `2`。

## 11. 狀態與效能文字

完成提示統一格式：

```text
7.4351 秒，30,123,123 節點，4,123,123 節點/秒
```

- 秒數固定 4 位小數。
- 節點數與每秒節點使用千分位整數。
- 達限制、搜尋中止、尚未證明最短或列舉未完整時，警告必須與效能資料一起顯示。

## 12. 驗收

搜尋核心變更至少必須通過：

1. Native C++ `-O3 -flto` 編譯與自我測試。
2. LTO SIMD Wasm 編譯。
3. Wasm ABI 匯出測試。
4. JavaScript 語法檢查。
5. 自由、無禁、有禁規則測試。
6. `single`、`multi`、`shortest`、`shortest-one` 模式測試。
7. `fast` 與 `strict` 剪枝結果驗證。
8. 時間、節點為 0 及非 0 的封裝與中止測試。
9. 限制中止時不誤報完整、最短或唯一。
10. 相同盤面與相同限制下的結果、節點數及耗時比較。

介面變更至少確認：

- 桌機與手機可操作。
- 搜尋中不能切換會改變語意的控制項。
- 「同型-活四取前一手」勾選與未勾選時，同一批活四終局路線會依不同同型 key 去重。
- 題目產生器不受該勾選項影響，固定保留活四手比較。
- 禁手圖層與其他分析圖層互不清除。
- 補子搜尋圈選最大 VCF 數字。
- 開啟 `/VCF/`、`/VCF/index.html` 或 `/VCF/makevcf.html` 均會轉往 `/VCF/rapfi/`，且不載入另一套舊版工作台。
- 相關 Markdown 與正式名稱、數值及流程一致。
