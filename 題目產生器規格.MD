# VCF 題目產生器規格

> 本文件只描述正式規格。舊流程、過時預設值、修改歷程與尚未採用的替代方案不得保留。

## 1. 執行架構

題目產生器整合在 `makevcf.html` 與 `/rapfi/` 工作台，共用主棋盤，但使用自己的 Bitboard Worker：

```text
makevcf-generator-core.js
  → rapfi/vcf-bitboard-worker.js
  → rapfi/engine/vcf-bitboard-engine.js / .wasm
```

規則：

- 不共用主分析頁的 Worker。
- 停止或重新啟動題目產生器時，不中止主分析。
- VCF 搜尋、路線精簡與防守點驗證都由題目產生器專用 Worker 執行。
- 模板枚舉使用所在頁面提供的同步棋型與禁手函式；`/rapfi/` 由 `rapfi/vcf-bitboard-generator-compat.js` 提供。
- 題目產生期間，主分析按鈕、棋盤、規則、攻方及會影響生成結果的控制項都必須鎖定。

## 2. 規則

規則值固定為：

| 值 | 名稱 | 規則 |
|---:|---|---|
| `0` | 自由 | 黑白長連皆算勝；黑白棋型皆取 9 格 |
| `1` | 無禁 | 黑白皆無禁手，長連不算勝也不算負；黑白棋型皆取 11 格 |
| `2` | 有禁 | 連珠規則；黑棋有禁手，黑棋取 11 格、白棋取 9 格 |

不得使用會把 `0` 當成預設值的真假值回退寫法。每次 `findVCF`、`trimVCFGroups`、`getBlockVCF` 及其他 Worker 驗證請求都必須明確附帶目前規則值。

## 3. 搜尋限制

題目產生器共用工作台的：

- 「時間限制」：單位秒。
- 「節點限制」：單位百萬節點。

規則：

- 題目產生開始時讀取兩個輸入值，整次產生使用同一份限制快照。
- 產生期間停用時間限制、節點限制及多組剪枝下拉選單。
- 時間或節點輸入 `0` 代表該項不限制。
- 不得再固定使用 `5,000,000` 節點，也不得在輸入為 `0` 時回退成 500 萬節點。
- 多組型搜尋使用工作台相同的 32-bit `maxNode` 封裝：

```text
bit 31      ：封裝標記
bit 0..9    ：節點上限，單位百萬；0 = 不限制
bit 10..30  ：時間上限，單位秒；0 = 不限制
```

- 搜尋時間每 524,288 節點檢查一次。
- 達時間或節點限制時可保留已找到結果，但必須標示搜尋未完整。

## 4. 搜尋模式與剪枝

一般候選驗證使用：

```text
mode = shortest
pruning = 目前多組剪枝下拉選單
maxVCF = 64
maxDepth = 2 × 目標步數 + 1 ply
maxNode = 工作台目前時間／節點限制的封裝值
```

「只保留目標 VCF」使用：

```text
mode = multi
pruning = 目前多組剪枝下拉選單
maxVCF = 64
maxDepth = 2 × 目標步數 + 1 ply
maxNode = 工作台目前時間／節點限制的封裝值
```

剪枝下拉選單：

```text
高速多組 VCF（集合子集） → pruning = fast
嚴格多組 VCF（完全同盤） → pruning = strict
```

規則：

- 每次題目搜尋送出前都以產生開始時取得的剪枝值為準。
- 呼叫端殘留的舊 `pruning` 參數不得覆蓋使用者選擇。
- 頁面沒有下拉選單時讀取 `vcf_multi_pruning`；沒有已儲存值時預設 `fast`。

## 5. VCF 步數

VCF 步數是**最終連五之前，攻方實際落子的手數**。

```text
攻方死四 → 守方擋 → 攻方活四 → 守方擋 → 攻方五連
```

以上為 2 步 VCF；最後連五那一手不計入。

同一手即使同時形成四四，也只算 1 步。程式必須重播手順計算，不得用形成的四數量代替落子手數。

題目可指定 1～10 步。搜尋最大深度為：

```text
2 × 目標步數 + 1 ply
```

## 6. N 點

N 點使用兩個獨立位元：

- `NO_BLACK`：黑方不能使用。
- `NO_WHITE`：白方不能使用。

同一點可同時具有兩種標記。

顯示：

- 綠色方塊：雙方 N 點。
- 紅框黑色方塊：黑方 N 點。
- 紅框白色方塊：白方 N 點。
- 方塊中心與棋盤交點重合。

完整目標 VCF 路線中的所有落點都加入雙方 N 點。

## 7. 初始材料

初始材料分為活三與死四。

### 7.1 活三

一般規則材料：

```text
留 留 攻 攻 攻 留
留 攻 留 攻 攻 留
```

- 三個「攻」為攻方棋，皆可作 A。
- 三個「留」為雙方 N 點。
- 中間「留」是形成活四的攻擊點。
- 最外側兩個「留」是五連點。
- 活三接一層新死四後形成 2 步基礎。

黑方有禁規則時，為避免外部非法延伸，增加黑方專用 N 點：

```text
留 留 攻 攻 攻 留 N
N 留 攻 留 攻 攻 留 N
```

其中額外的 `N` 只禁止黑方使用；位於盤外時不建立標記。

### 7.2 死四

```text
X 攻 攻 攻 攻 留
攻 攻 攻 留 攻
攻 攻 留 攻 攻
```

死四一：

- X 為盤端或守方棋。
- X 在盤內時為攻方 N 點。
- 靠近 X 的攻方棋不能作 A。

死四二、三：

- 最外側兩顆攻方棋不能作 A。
- 靠近內部「留」的攻方棋可作 A。

所有「留」均為雙方 N 點。死四接一層新死四時，A 下回同時完成兩個四，形成 1 步 VCF。

有禁規則且黑方進攻時，四四為禁手，因此不使用死四基礎；黑方有禁規則沒有合法 1 步題目。

## 8. 新死四模板

```text
1. X 五 攻 攻 攻 攻 X
2. X 攻 五 攻 攻 攻 X
3. X 攻 攻 五 攻 攻 X
```

- `攻`：攻方棋。
- `五`：死四形成後守方唯一被迫防守點。
- `X`：模板外側位置。
- A 必須是盤面既有攻方棋，並與模板一個攻位重合。
- 完整套入模板後移除 A，A 成為新題目的第一手。
- 四個方向及正反方向都可枚舉，不人工限制必須轉向。

## 9. 模板相容規則

### 9.1 攻方棋位置

可沿用既有攻方棋，或在不是攻方 N 點的空點新增攻方棋；不能覆蓋守方棋。

### 9.2 A 點

- 必須是原盤面既有攻方棋。
- 不能是初始材料標記為禁止 A 的棋。
- 不能是攻方 N 點。
- A 不計入沿用棋子加成。

### 9.3 五點

- 不能是攻方 N 點。
- 不能已有攻方棋。
- 可為空點或原本的守方棋。
- 原本是守方棋時，生成期間暫時移除，VCF 過程中由守方下回。
- 原有守方棋視為沿用棋子，計入沿用棋子加成。

### 9.4 X 點

X 可為空點、守方棋、N 點或盤端；不能已有攻方棋。X 原有守方棋視為沿用棋子。

### 9.5 同線四四

新死四與原死四同方向時，A 下回必須形成單線四四，且該線恰有兩個不同五點。四四仍只算 1 步。

### 9.6 新死四產生活三

套入完整死四並移除 A 後，掃描新死四範圍。若攻方在某空點合法落子可於該方向形成活四，視為存在活三。

存在活三時：

1. 只允許在左右 X 的可用空點補守方棋。
2. 補入點不能是守方 N 點。
3. 枚舉所有非空 X 組合：左、右、兩端。
4. 補後仍有活三的方案淘汰。
5. 只保留補守子數最少的有效方案；同樣最少時可保留多個候選。

## 10. 指定步數生成

### 10.1 1 步

1. 只使用死四基礎。
2. 在基礎外增加一層死四並移除 A。
3. A 下回必須合法形成四四。
4. 不得存在更短取勝路線。
5. 有禁規則且黑方進攻時沒有合法候選。

### 10.2 2～10 步

1. 建立規則允許的活三與死四基礎。
2. 活三與死四皆可用時，先等機率選材料類別，再依該類候選權重抽選。
3. 活三加一層死四形成 2 步基礎。
4. 死四加一層死四形成 1 步基礎。
5. 每次延伸選盤面既有攻方棋作 A，再增加一層死四。
6. 每成功延伸一層，VCF 步數必須正好增加 1。
7. 候選失敗時回溯嘗試同層其他候選；整個基礎無法延伸時重建基礎。

層數：

```text
活三路線新增死四層數 = 目標步數 - 1
死四路線新增死四層數 = 目標步數
```

## 11. 標準完成盤面

延伸驗證不比較落子順序，只比較 225 格黑白棋位置。

- 終局為四四：記錄四四形成後的盤面。
- 終局為活四：記錄攻方下出活四之前的盤面。
- 終局為連五：記錄連五形成後的盤面。
- 其他終局：記錄終局棋型完成時的盤面。

新增一層後，相較前一層可永久增加：

- 本層新增攻方棋。
- 封閉活三或封鎖較短 VCF 所加入的守方棋。
- 最後為補齊黑白子數加入的守方棋。

A 與原五點守方棋只是在生成時暫時移除，VCF 過程中下回，不算永久新增差異。

## 12. 一般 VCF 驗證

流程：

1. 以 `shortest` 搜尋目前最短的一批 VCF。
2. 重播每條路線，計算題目步數與標準完成盤面。
3. 若最短步數等於目標，至少一條路線必須符合預期完成盤面。
4. 若最短步數小於目標：
   - 未勾選「補齊黑白子數」時直接淘汰。
   - 勾選時進入較短 VCF 自動補守。
5. 沒有 VCF、最短步數高於目標或最短層沒有預期完成盤面時淘汰。
6. 搜尋因限制中止時，不得把目前結果描述為完整最短層證明。

## 13. 較短 VCF 自動補守

「補齊黑白子數」預設開啟，除了最後補齊子數，也負責封鎖比目標短的 VCF。

流程：

1. 對目前最短層每條較短 VCF 計算防守點。
2. 統計可同時封鎖較多較短路線的守方落點。
3. 排除 N 點、禁手、守方形成四、已占用點及受保護的目標防守點。
4. 加入一顆守方棋後，以相同搜尋限制與剪枝重新執行 `shortest`。
5. 重複直到最短層等於目標，並找到預期完成盤面。

限制：

- 每層最多嘗試 6 個防守點。
- 每個候選最多展開 42 個補守狀態。
- 目標路線已出現在目前最短層時，不可使用會封鎖目標的點。
- 目標尚未出現在目前最短層時可先封鎖較短路線，但每個分支最後仍必須重新證明目標存在。
- 可加入的守方棋數不得超過正常輪到攻方下棋所允許的數量。

## 14. 補齊黑白子數

目標題目完成後，依正常輪到攻方下棋所需的黑白子數補守方棋。

候選點：

- 必須在棋盤內圈。
- 必須為空點。
- 不能是守方 N 點。
- 不能是目前目標 VCF 的防守點。
- 守方落子不能形成四或連五。
- 有禁規則且守方為黑棋時不得為禁手。

每補一顆守方棋後，都以相同搜尋限制及剪枝重新執行 `shortest`，確認：

- 不出現比目標短的 VCF。
- 目標步數仍存在。
- 標準完成盤面等於原目標盤面加上該守方棋。

限制：

- 每層最多嘗試 10 個補子候選。
- 最多展開 140 個補子狀態。

補子權重：

- 攻方形成活四權重最高。
- 攻方形成死四次之。
- 攻守雙方形成活三或死三時乘上「三型加成」。
- 三型加成預設 30 倍，可輸入 0～1,000,000。
- 最後乘以該點周圍兩格內棋子數，使盤面較集中。

## 15. 只保留目標 VCF

目標路線必須同時符合：

- 題目步數等於目標。
- 標準完成盤面等於預期盤面。

其他路線以守方棋逐步封鎖：

- 每個狀態最多嘗試 8 個防守點。
- 最多展開 72 個封鎖狀態。
- 不可封鎖目標路線。

唯一性成立條件：

- 至少保留一條目標路線。
- 不存在任何非目標路線。
- `aborted` 必須為 `false`。
- 回傳路線數必須小於 64。
- 不得達到本次產生所使用的時間限制。
- 不得達到本次產生所使用的節點限制。

達限制時沒有看到其他路線，不代表已證明唯一。

## 16. 候選權重與排序

候選先完成枚舉、棋型篩選及去重，再依控制項決定驗證順序。

### 16.1 沿用棋子加成

- 預設 10%。
- A 不計入。
- 計算新死四另外三個攻位直接沿用的攻方棋。
- 計算模板 X 點原有的守方棋。
- 計算五點原有、生成時暫時移除的守方棋。
- 每沿用一顆增加一次 `99 × 百分比 ÷ 100`。

### 16.2 棋子集中加成

- 預設 15%。
- 以目前盤面全部黑白棋座標的平均值作為分布中心。
- 取新死四七格模板的中央點。
- 中央點越接近分布中心，分數越高。
- 滿分加成為 `99 × 百分比 ÷ 100`。

### 16.3 最終權重

```text
候選權重
= 1
+ 沿用棋子數 × 99 × 沿用百分比 ÷ 100
+ 棋子集中分數 × 99 × 集中百分比 ÷ 100
```

### 16.4 排序模式

- 未勾選「依加成高低排序」：依候選權重做加權隨機排序。
- 勾選：依總權重由高到低逐一驗證；同權重隨機排列。

權重與排序只影響嘗試順序，不放寬 N 點、禁手、棋型、步數、完成盤面或其他 VCF 驗證。

## 17. 結果與摘要

產生成功後保存：

- 最終題目盤面。
- 攻方與規則。
- 目標 VCF 路線。
- 題目步數。
- 完成盤面與標準完成盤面。
- 每層 A、五點、方向、模板及永久新增攻守棋。
- N 點。
- 自動封鎖較短 VCF 的守方棋。
- 最後補齊子數的守方棋。
- 最後一次成功驗證搜尋的節點數。
- 最後一次成功驗證回傳的路線組數。

`nodeCount` 不是整次題目產生期間所有候選的累計節點數，不得標示為總節點數。

摘要顯示：

- 初始棋型。
- 活三封閉、較短 VCF 封鎖及最後補齊數量。
- 每層沿用攻子數與合計。
- 每層候選組數。
- 最終黑白子數。
- 最後一次多組 VCF 路線數。

「顯示答案」切換目標路線；「顯示 N 點」切換 N 點圖層。

## 18. 驗收

每次修改至少確認：

1. 所有 `makevcf-generator-*.js` 通過 JavaScript 語法檢查。
2. 題目產生器建立自己的 `rapfi/vcf-bitboard-worker.js`。
3. Worker 正確載入 `rapfi/engine/vcf-bitboard-engine.js`。
4. 主分析 Worker 不被題目產生器取消或重啟。
5. 自由、無禁、有禁分別以 `0`、`1`、`2` 傳入所有搜尋、精簡及防守請求。
6. 一般驗證送出 `shortest`。
7. 「只保留目標 VCF」送出 `multi`。
8. 下拉選單選 `fast` 時送出高速剪枝，選 `strict` 時送出嚴格剪枝。
9. 時間、節點、深度及 64 組限制正確傳入。
10. 時間或節點為 `0` 時保持不限制，不回退成 500 萬。
11. 題目產生期間規則、攻方、步數、搜尋限制、剪枝及權重控制項無法切換。
12. `getBlockVCF` 正確取得 `points`。
13. 自動補守後仍能找到目標步數及預期完成盤面。
14. 唯一題達任何限制時不會誤判唯一。
15. 手機版可操作，文件與正式程式使用相同名稱、數值及流程。
