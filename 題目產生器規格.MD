# VCF 題目產生器規格

> 本文件只描述正式規格。舊流程、過時預設值、修改歷程與尚未採用的替代方案不得保留。

## 1. 執行架構

題目產生器整合在 `makevcf.html` 與 `/rapfi/` 工作台，共用主棋盤，但使用自己的 Bitboard Worker：

```text
makevcf-generator-core.js
  → rapfi/vcf-bitboard-worker.js
  → rapfi/engine/vcf-bitboard-engine.js / .wasm
```

規則：

- 不共用主分析頁的 Worker。
- 停止或重新啟動題目產生器時，不中止主分析。
- VCF 搜尋、路線精簡與防守點驗證都由題目產生器專用 Worker 執行。
- 模板枚舉使用所在頁面提供的同步棋型與禁手函式；`/rapfi/` 由 `rapfi/vcf-bitboard-generator-compat.js` 提供。
- 題目產生期間，主分析按鈕、棋盤、規則、攻方及會影響生成結果的控制項都必須鎖定。

## 2. 規則

規則值固定為：

| 值 | 名稱 | 規則 |
|---:|---|---|
| `0` | 自由 | 黑白長連皆算勝；黑白棋型皆取 9 格 |
| `1` | 無禁 | 黑白皆無禁手，長連不算勝也不算負；黑白棋型皆取 11 格 |
| `2` | 有禁 | 連珠規則；黑棋有禁手，黑棋取 11 格、白棋取 9 格 |

不得使用會把 `0` 當成預設值的真假值回退寫法。每次 `findVCF`、`trimVCFGroups`、`getBlockVCF` 及其他 Worker 驗證請求都必須明確附帶目前規則值。

## 3. 搜尋限制

題目產生器共用工作台的「時間限制」與「節點限制」。

- 題目產生開始時讀取兩個輸入值，整次產生使用同一份限制快照。
- 產生期間停用時間限制、節點限制及多組剪枝下拉選單。
- 時間或節點輸入 `0` 代表該項不限制。
- 不得固定回退成 500 萬節點。
- 多組型搜尋使用工作台相同的 32-bit `maxNode` 封裝：

```text
bit 31      ：封裝標記
bit 0..9    ：節點上限，單位百萬；0 = 不限制
bit 10..30  ：時間上限，單位秒；0 = 不限制
```

- 搜尋時間每 524,288 節點檢查一次。
- 達時間或節點限制時可保留已找到結果，但必須標示搜尋未完整。

## 4. 搜尋模式與剪枝

一般候選驗證使用：

```text
mode = shortest
pruning = 目前多組剪枝下拉選單
maxVCF = 64
maxDepth = 2 × 目標步數 + 1 ply
maxNode = 工作台目前時間／節點限制的封裝值
```

「只保留目標 VCF」使用：

```text
mode = multi
pruning = 目前多組剪枝下拉選單
maxVCF = 64
maxDepth = 2 × 目標步數 + 1 ply
maxNode = 工作台目前時間／節點限制的封裝值
```

剪枝下拉選單：

```text
高速多組 VCF（集合子集） → pruning = fast
嚴格多組 VCF（完全同盤） → pruning = strict
```

- 每次題目搜尋送出前都以產生開始時取得的剪枝值為準。
- 呼叫端殘留的舊 `pruning` 參數不得覆蓋使用者選擇。
- 頁面沒有下拉選單時讀取 `vcf_multi_pruning`；沒有已儲存值時預設 `fast`。

## 5. VCF 步數

VCF 步數是**最終連五或抓禁手成立之前，攻方實際落子的手數**。

```text
攻方死四 → 守方擋 → 攻方活四 → 守方擋 → 攻方五連
```

以上為 2 步 VCF；最後連五那一手不計入。同一手即使同時形成四四，也只算 1 步。程式必須重播手順計算，不得用形成的四數量代替落子手數。

白方抓禁手中，白方下出最後死四、使黑方唯一防點為禁手的那一手計入步數；非法的黑方禁手落子不會出現在合法答案手順中。

題目可指定 1～10 步，搜尋最大深度為 `2 × 目標步數 + 1 ply`。

## 6. N 點

N 點使用兩個獨立位元：

- `NO_BLACK`：黑方不能使用。
- `NO_WHITE`：白方不能使用。

同一點可同時具有兩種標記。

顯示：

- 綠色方塊：雙方 N 點。
- 紅框黑色方塊：黑方 N 點。
- 紅框白色方塊：白方 N 點。
- 方塊中心與棋盤交點重合。

完整目標 VCF 路線中的所有落點都加入雙方 N 點。白方抓禁手的最終禁點 A 固定為雙方 N 點；模板生成時可暫時解除該標記以套用既有死四程式，候選建立後必須立即恢復。

## 7. 初始材料

一般題型的初始材料分為活三與死四；白方有禁規則時另可使用抓禁手基礎。

### 7.1 活三

一般規則材料：

```text
留 留 攻 攻 攻 留
留 攻 留 攻 攻 留
```

- 三個「攻」為攻方棋，皆可作 A。
- 三個「留」為雙方 N 點。
- 中間「留」是形成活四的攻擊點。
- 最外側兩個「留」是五連點。
- 活三接一層新死四後形成 2 步基礎。

黑方有禁規則時增加黑方專用 N 點：

```text
留 留 攻 攻 攻 留 N
N 留 攻 留 攻 攻 留 N
```

額外的 `N` 只禁止黑方使用；位於盤外時不建立標記。

### 7.2 死四

```text
X 攻 攻 攻 攻 留
攻 攻 攻 留 攻
攻 攻 留 攻 攻
```

死四一：

- X 為盤端或守方棋。
- X 在盤內時為攻方 N 點。
- 靠近 X 的攻方棋不能作 A。

死四二、三：

- 最外側兩顆攻方棋不能作 A。
- 靠近內部「留」的攻方棋可作 A。

所有「留」均為雙方 N 點。死四接一層新死四時，A 下回同時完成兩個四，形成 1 步 VCF。

有禁規則且黑方進攻時，四四為禁手，因此不使用死四基礎；黑方有禁規則沒有合法 1 步題目。

### 7.3 白方抓禁手基礎

只在 `規則 = 2` 且攻方為白方時使用。每次建立基礎時，長連、四四、三三三類以相同機率選擇；若所選類型當次無有效候選，重新抽選並建立新骨架。

共同流程：

1. 建立黑棋禁手骨架，最終禁點記為 A。
2. 移除 A 上用於建立骨架的暫時黑棋。
3. A 設為雙方 N 點。
4. 使用既有新死四模板建立白棋死四，並令其五點固定為 A。
5. 白棋下回後必須形成單一死四，不得把白棋四四或活四誤當抓禁手基礎。
6. 在「白棋下回後、黑棋尚未落 A」的實際盤面呼叫正式禁手判斷；黑 A 必須確實為禁手。
7. 不要求禁手類型互斥；例如同時符合四四與三三仍可接受。

#### 7.3.1 長連

模板：

```text
黑 A 黑 黑 黑 黑 X
黑 黑 A 黑 黑 黑 N N
```

- 第一式的 X 必須為白棋或盤端。
- 第二式的 N 為黑方 N 點，位於盤外時不建立標記。
- 黑棋落 A 後必須由正式禁手判斷確認為禁手。

#### 7.3.2 四四

- 第一個四沿用既有死四基礎。
- 第二個四沿用既有新死四／四四生成程式與同線雙四判斷。
- 可生成不同方向四四，也可生成同一直線、具有兩個不同五點的同線四四。
- 建立黑方禁手骨架時允許 A 為禁手；完成骨架後再以正式禁手判斷確認。
- 第一個四移除交點 A 後，即使退化成活三，也**不補白棋守子封閉**。此例外只適用於抓禁手的黑方四四骨架；一般新死四仍沿用第 9.6 節的活三補守流程。

#### 7.3.3 三三

沿用有禁黑方活三材料：

```text
留 留 黑 黑 黑 留 N
N 留 黑 留 黑 黑 留 N
```

- 「留」為雙方 N 點，外側 N 為黑方 N 點。
- 任取兩個不同方向的活三，使兩者的其中一顆黑棋重合於 A，再移除 A。
- 合併後不得讓既有黑棋落在黑方 N 點。
- 黑棋落 A 後必須由正式禁手判斷確認為禁手。

## 8. 新死四模板

```text
1. X 五 攻 攻 攻 攻 X
2. X 攻 五 攻 攻 攻 X
3. X 攻 攻 五 攻 攻 X
```

- `攻`：攻方棋。
- `五`：死四形成後守方唯一被迫防守點。
- `X`：模板外側位置。
- A 必須是盤面既有攻方棋，並與模板一個攻位重合。
- 完整套入模板後移除 A，A 成為新題目的第一手。
- 四個方向及正反方向都可枚舉，不人工限制必須轉向。
- 抓禁手基礎建立白死四時，「五」固定為黑方禁點 A。

## 9. 模板相容規則

### 9.1 攻方棋位置

可沿用既有攻方棋，或在不是攻方 N 點的空點新增攻方棋；不能覆蓋守方棋。

### 9.2 A 點

- 必須是原盤面既有攻方棋。
- 不能是初始材料標記為禁止 A 的棋。
- 不能是攻方 N 點。
- A 不計入沿用棋子加成。

抓禁手中的「最終黑方禁點 A」是白死四的五點，不是白方反向延伸層的攻擊 A；其 N 點規則依第 6、7.3 節處理。

### 9.3 五點

- 不能是攻方 N 點。
- 不能已有攻方棋。
- 可為空點或原本的守方棋。
- 原本是守方棋時，生成期間暫時移除，VCF 過程中由守方下回。
- 原有守方棋視為沿用棋子，計入沿用棋子加成。
- 抓禁手最內層的五點 A 為禁手，不會由黑方合法下回；其標準完成盤面停在白方死四成立時。

### 9.4 X 點

X 可為空點、守方棋、N 點或盤端；不能已有攻方棋。X 原有守方棋視為沿用棋子。

### 9.5 同線四四

新死四與原死四同方向時，A 下回必須形成單線四四，且該線恰有兩個不同五點。四四仍只算 1 步。白方抓禁手的黑方四四骨架沿用相同判斷。

### 9.6 新死四產生活三

套入完整死四並移除 A 後，掃描新死四範圍。若攻方在某空點合法落子可於該方向形成活四，視為存在活三。

存在活三時：

1. 只允許在左右 X 的可用空點補守方棋。
2. 補入點不能是守方 N 點。
3. 枚舉所有非空 X 組合：左、右、兩端。
4. 補後仍有活三的方案淘汰。
5. 只保留補守子數最少的有效方案；同樣最少時可保留多個候選。

唯一例外：建立白方抓禁手的黑方四四骨架時，第一個四移除禁點 A 所留下的活三不補守子。

## 10. 指定步數生成

### 10.1 1 步

一般題型：

1. 使用死四基礎。
2. 在基礎外增加一層死四並移除 A。
3. A 下回必須合法形成四四。
4. 不得存在更短取勝路線。
5. 有禁規則且黑方進攻時沒有合法候選。

白方有禁規則：

1. 建立第 7.3 節的抓禁手基礎。
2. 白方下出死四後，黑方唯一防點為禁點 A。
3. 合法 VCF 答案停在白方死四成立；非法黑 A 不列入答案手順。
4. 搜尋結果必須重播為正好 1 步，且標準完成盤面符合預期。

### 10.2 2～10 步

一般題型：

1. 建立規則允許的活三與死四基礎。
2. 活三與死四皆可用時，先等機率選材料類別，再依該類候選權重抽選。
3. 活三加一層死四形成 2 步基礎。
4. 死四加一層死四形成 1 步基礎。
5. 每次延伸選盤面既有攻方棋作 A，再增加一層死四。
6. 每成功延伸一層，VCF 步數必須正好增加 1。
7. 候選失敗時回溯嘗試同層其他候選；整個基礎無法延伸時重建基礎。

白方有禁規則：

1. 先建立 1 步抓禁手基礎。
2. 再使用既有反向新死四程式增加 `目標步數 - 1` 層。
3. 每層成功後仍須重新搜尋，證明最短 VCF 正好增加 1 步。
4. 多步延伸後仍必須保留原最終禁點 A、禁手類型與正式禁手驗證。

一般層數：

```text
活三路線新增死四層數 = 目標步數 - 1
死四路線新增死四層數 = 目標步數
抓禁手路線外加死四層數 = 目標步數 - 1
```

## 11. 標準完成盤面

延伸驗證不比較落子順序，只比較 225 格黑白棋位置。

- 終局為四四：記錄四四形成後的盤面。
- 終局為活四：記錄攻方下出活四之前的盤面。
- 終局為連五：記錄連五形成後的盤面。
- 終局為抓禁手：記錄白方下出最終死四後、黑方尚未非法落 A 的盤面。
- 其他終局：記錄終局棋型完成時的盤面。

新增一層後，相較前一層可永久增加：

- 本層新增攻方棋。
- 封閉活三或封鎖較短 VCF 所加入的守方棋。
- 最後為補齊黑白子數加入的守方棋。

一般題型的 A 與原五點守方棋只是在生成時暫時移除，VCF 過程中下回，不算永久新增差異。抓禁手最內層的禁點 A 始終保持空點。

## 12. 一般 VCF 驗證

1. 以 `shortest` 搜尋目前最短的一批 VCF。
2. 重播每條路線，計算題目步數與標準完成盤面。
3. 若最短步數等於目標，至少一條路線必須符合預期完成盤面。
4. 若最短步數小於目標：未勾選「補齊黑白子數」時淘汰；勾選時進入較短 VCF 自動補守。
5. 沒有 VCF、最短步數高於目標或最短層沒有預期完成盤面時淘汰。
6. 搜尋因限制中止時，不得把目前結果描述為完整最短層證明。
7. 抓禁手候選在模板建立及最終預期盤面驗證時，都必須重新確認黑 A 為禁手。

## 13. 較短 VCF 自動補守

「補齊黑白子數」預設開啟，除了最後補齊子數，也負責封鎖比目標短的 VCF。

流程：

1. 對目前最短層每條較短 VCF 計算防守點。
2. 統計可同時封鎖較多較短路線的守方落點。
3. 排除 N 點、禁手、守方形成四、已占用點及受保護的目標防守點。
4. 加入一顆守方棋後，以相同搜尋限制與剪枝重新執行 `shortest`。
5. 重複直到最短層等於目標，並找到預期完成盤面。

限制：

- 每層最多嘗試 6 個防守點。
- 每個候選最多展開 42 個補守狀態。
- 目標路線已出現在目前最短層時，不可使用會封鎖目標的點。
- 目標尚未出現在目前最短層時可先封鎖較短路線，但每個分支最後仍必須重新證明目標存在。
- 可加入的守方棋數不得超過正常輪到攻方下棋所允許的數量。
- 抓禁手的最終禁點 A 為雙方 N 點，不可被自動補守占用。

## 14. 補齊黑白子數

目標題目完成後，依正常輪到攻方下棋所需的黑白子數補守方棋。

候選點：

- 必須在棋盤內圈。
- 必須為空點。
- 不能是守方 N 點。
- 不能是目前目標 VCF 的防守點。
- 守方落子不能形成四或連五。
- 有禁規則且守方為黑棋時不得為禁手。

每補一顆守方棋後，都以相同搜尋限制及剪枝重新執行 `shortest`，確認不出現較短 VCF、目標步數仍存在、標準完成盤面符合預期。

限制：

- 每層最多嘗試 10 個補子候選。
- 最多展開 140 個補子狀態。

補子權重：

- 攻方形成活四權重最高。
- 攻方形成死四次之。
- 攻守雙方形成活三或死三時乘上「三型加成」。
- 三型加成預設 30 倍，可輸入 0～1,000,000。
- 最後乘以該點周圍兩格內棋子數，使盤面較集中。

## 15. 只保留目標 VCF

目標路線必須同時符合題目步數與預期標準完成盤面。其他路線以守方棋逐步封鎖：

- 每個狀態最多嘗試 8 個防守點。
- 最多展開 72 個封鎖狀態。
- 不可封鎖目標路線。

唯一性成立條件：

- 至少保留一條目標路線。
- 不存在任何非目標路線。
- `aborted` 必須為 `false`。
- 回傳路線數必須小於 64。
- 不得達到本次產生所使用的時間限制或節點限制。

達限制時沒有看到其他路線，不代表已證明唯一。

## 16. 候選權重與排序

候選先完成枚舉、棋型篩選及去重，再依控制項決定驗證順序。

### 16.1 沿用棋子加成

- 預設 10%。
- A 不計入。
- 計算新死四另外三個攻位直接沿用的攻方棋。
- 計算模板 X 點原有的守方棋。
- 計算五點原有、生成時暫時移除的守方棋。
- 每沿用一顆增加一次 `99 × 百分比 ÷ 100`。

### 16.2 棋子集中加成

- 預設 15%。
- 以目前盤面全部黑白棋座標的平均值作為分布中心。
- 取新死四七格模板的中央點。
- 中央點越接近分布中心，分數越高。
- 滿分加成為 `99 × 百分比 ÷ 100`。

### 16.3 最終權重

```text
候選權重
= 1
+ 沿用棋子數 × 99 × 沿用百分比 ÷ 100
+ 棋子集中分數 × 99 × 集中百分比 ÷ 100
```

### 16.4 排序模式

- 未勾選「依加成高低排序」：依候選權重做加權隨機排序。
- 勾選：依總權重由高到低逐一驗證；同權重隨機排列。

權重與排序只影響嘗試順序，不放寬 N 點、禁手、棋型、步數、完成盤面或其他 VCF 驗證。

## 17. 結果與摘要

產生成功後保存：

- 最終題目盤面、攻方、規則、目標 VCF 路線與題目步數。
- 完成盤面與標準完成盤面。
- 每層 A、五點、方向、模板及永久新增攻守棋。
- N 點、自動封鎖較短 VCF 的守方棋及最後補齊子數的守方棋。
- 抓禁手題的最終禁點 A、禁手類型與模板資訊。
- 最後一次成功驗證搜尋的節點數與路線組數。

`nodeCount` 不是整次題目產生期間所有候選的累計節點數，不得標示為總節點數。

摘要顯示：

- 初始棋型；抓禁手題顯示「抓禁手（長連／四四／三三，A=座標）」。
- 活三封閉、較短 VCF 封鎖及最後補齊數量。
- 每層沿用攻子數與合計。
- 每層候選組數。
- 最終黑白子數。
- 最後一次多組 VCF 路線數。

「顯示答案」切換目標路線；「顯示 N 點」切換 N 點圖層。

## 18. 驗收

每次修改至少確認：

1. 所有 `makevcf-generator-*.js` 通過 JavaScript 語法檢查。
2. 題目產生器建立自己的 `rapfi/vcf-bitboard-worker.js`，且不取消主分析 Worker。
3. 自由、無禁、有禁分別以 `0`、`1`、`2` 傳入所有搜尋、精簡及防守請求。
4. 一般驗證送出 `shortest`；「只保留目標 VCF」送出 `multi`。
5. 剪枝、時間、節點、深度及 64 組限制正確傳入；輸入 `0` 時保持不限制。
6. 題目產生期間相關控制項無法切換。
7. 自動補守後仍能找到目標步數及預期完成盤面；唯一題達限制時不誤判唯一。
8. 白方有禁規則可產生長連、四四、三三三類抓禁手題。
9. 每個抓禁手候選的 A 都是雙方 N 點，白方最終死四的五點等於 A。
10. 黑棋落 A 經正式禁手判斷為禁手，不僅依模板外觀接受。
11. 四四可生成異線與同線雙四，且同線時具有兩個不同五點。
12. 建立黑方四四骨架時，移除 A 所產生的活三不補守子；一般死四的活三修補不受影響。
13. 目標步數大於 1 時，抓禁手基礎外正好增加 `目標步數 - 1` 層死四，並保留原禁點與禁手資料。
14. 手機版可操作，文件與正式程式使用相同名稱、數值及流程。
