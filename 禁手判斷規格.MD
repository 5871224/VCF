# 禁手判斷現行規格

適用核心：`rapfi/vcf-pattern-engine-optimized.cpp`

本文件只描述目前正式的棋型與禁手判斷流程。

## 1. 對外函式

### `vcfAnalyzePoint`

同時回傳落點的四方向棋型、合併棋型與禁手結果。

- 四方向棋型只計算一次。
- 第一次棋型結果直接供禁手初判使用。
- 不得再為同一落點重算四方向棋型。

### `vcfAnalyzeForbidden`

專門判斷黑棋落點是否為禁手，供只需要合法性結果的功能使用。

輸出固定為 4 bytes：

```text
forbidden
forbiddenType
realThreeDirections
reserved
```

## 2. 頂層禁手判斷順序

```text
第一次四方向棋型判斷
  → 正五：合法，立即返回
  → 長連：長連禁手，立即返回
  → 同線雙四或兩個四：四四禁手，立即返回
  → 不足兩個疑似活三：合法，立即返回
  → 疑似三三：進入真假活三遞迴驗證
```

長連與四四只能使用第一次落點的四方向結果判斷，不得進入後續逐點驗證。

## 3. 疑似三三遞迴架構

只有排除正五、長連與四四後，仍有至少兩個 `F3`／`F3S` 方向時，才執行後續驗證。

流程：

1. 複製完整 225 格棋盤。
2. 在副本暫時落下目前待判斷的黑棋。
3. 取得各疑似活三方向的「活四延伸點遮罩」。
4. 每個疑似活三方向逐一檢查其活四延伸點。
5. 任一活四延伸點可下，該方向即為真活三；所有活四延伸點皆不可下，該方向為假三。
6. 已確認兩個真活三時，立即判定三三禁手。
7. 已確認真活三數加上尚未驗證的可能方向數仍小於二時，立即判定非禁手。
8. 否則繼續驗證下一個疑似活三方向。

最外層保留既有 `realThreeDirections` 的精確輸出；遞迴內層只需要禁／非禁結果，符合第 7 點時立即剪枝。

公開函式不得修改呼叫端傳入的棋盤。

## 4. 活四延伸點是否可下

活四延伸點由該疑似活三方向的 `openFourMask` 產生，因此補子後原方向已確定為活四，遞迴層不得重算原方向。

在活四延伸點暫時補上黑棋後，只判斷另外三個方向：

```text
任一方向已有五連或長連
  → 此活四延伸點不可下

任一方向存在至少一個合法連五點
  → 此活四延伸點不可下
  → 原方向已有活四，因此已形成第二個四

其他三方向不足兩個疑似活三
  → 此活四延伸點可下

其他三方向仍有至少兩個疑似活三
  → 使用目前已補子的盤面遞迴判斷
  → 遞迴結果為禁手：此活四延伸點不可下
  → 遞迴結果為非禁手：此活四延伸點可下
```

這裡的「存在合法連五點」不是既有單一棋型代碼 `B4`，而是專用布林資料：只要該方向至少有一個空點，補黑後能形成正五，即視為存在第二個四；不需要區分死四、活四或同線有幾個四。

在活三定義中，活四延伸點若同時在其他方向形成正五，也不能作為該活三的有效活四點；這與一般盤面上正五屬合法勝棋的規則不同。遞迴層不得套用頂層「正五合法」的捷徑。

## 5. 遞迴專用方向資料

遞迴層不得呼叫完整 `classifyPoint()`，只使用 Renju 黑棋專用方向快取：

```text
hasFiveRun
hasFiveCompletion
openFourMask
```

- `hasFiveRun`：目前中心落子後，該方向已有連續五顆以上黑棋。
- `hasFiveCompletion`：該方向至少存在一個補黑後形成正五的空點。
- `openFourMask`：若該方向為疑似活三，列出補黑後形成活四的所有相對位置。

快取以 10 格三進位方向 key 為索引，按實際遇到的 key 延遲建立。每個 key 只建立一次。

遞迴判斷一個活四延伸點時：

- 完全跳過已知形成活四的原方向。
- 另外三個方向各只編碼及查表一次。
- 已取得的 `openFourMask` 必須直接傳入下一層，不得在遞迴入口重算。

## 6. 棋盤複製規則

以下情況不得複製完整棋盤：

- 白棋。
- 非連珠規則。
- 一般合法黑棋落點。
- 正五。
- 長連禁手。
- 四四禁手。

只有疑似三三需要可變棋盤時，才複製 225 格棋盤。遞迴層沿用同一份副本，暫時補子後必須在返回前復原。

## 7. 建置入口

正式 Native 與 WebAssembly 建置都使用：

```text
rapfi/vcf-pattern-engine-optimized.cpp
```

該檔案包含並沿用 `vcf-pattern-engine.cpp` 的頂層查表與棋型分類核心，再提供優化後的公開函式及遞迴專用方向快取。

## 8. 驗收

必須通過：

- 三種頂層棋型查表方法結果一致。
- 遞迴專用方向表與原始棋型定義逐 key 對照一致。
- 優化前後隨機盤面禁手輸出完全一致。
- `vcfAnalyzeForbidden` 與 `vcfAnalyzePoint` 的禁手結果一致。
- 正五、長連、四四、真三三與假三三測試。
- 呼叫禁手函式後原始棋盤完全不變。
- Native C++、Bitboard 搜尋核心與 WebAssembly 編譯測試。

## 9. 顯示禁手介面

`/rapfi/` 規則區提供「顯示禁手」勾選項，只在「有禁手」規則下生效。

勾選後掃描所有空點並標示黑棋禁手：

- 長連禁手顯示 `6`。
- 四四禁手顯示 `44`。
- 三三禁手顯示 `33`。

顯示規則：

- 每次落子、移除棋子、清空棋盤、載入題目或匯入盤面後都必須重新計算。
- 切換規則或取消勾選時立即清除標示。
- 標示使用獨立 SVG 圖層，不得清除或覆寫 VCF 路線與其他分析圖層。
- 掃描在獨立 Worker 執行，只有勾選後才載入棋型 WebAssembly。
- 新盤面產生後，尚未完成的舊掃描結果不得顯示。
- 勾選狀態保存在瀏覽器 `localStorage`。
