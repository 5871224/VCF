name: Apply branch jump v4

on:
  push:
    paths:
      - .github/workflows/apply-branch-jump-v4.yml
      - tools/apply-branch-jump-v4.py

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply patch
        run: python3 tools/apply-branch-jump-v4.py
      - name: Verify
        run: |
          node --check makevcf-layout.js
          node --check rapfi/rapfi-workbench-header.js
          npm test
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add makevcf-layout.js rapfi/rapfi-workbench-header.js makevcf.html tests/workbench-architecture.test.js 規格書.MD
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "修正分岔點導覽與多下一手提示"
          git push origin HEAD:main
