# 活四不搜後續規格

## 目的

多組 VCF 搜尋在同一盤面已經存在直接連五或活四勝法時，原本仍會繼續展開後面的單防死四候選，因而產生「已有活三可直接下成活四，卻先下成死四再轉往其他勝法」的延伸路線。

「活四不搜後續」提供可選的節點內支配剪枝，讓使用者決定是否保留這類延伸勝法。

## 介面

- 勾選項名稱固定為「活四不搜後續」。
- 預設不勾選，維持原本完整列舉行為。
- 設定保存於 `localStorage` 的 `vcf_stop_after_open_four`。
- 搜尋執行期間停用此勾選項，避免同一次搜尋中途改變規則。
- 快速剪枝與嚴格剪枝共用同一個勾選項。

## 適用範圍

勾選後套用於：

- `multi` 多組 VCF。
- `shortest` 多組逐層搜尋。
- 題目產生器使用上述兩種搜尋模式時。

不套用於：

- `single` 最速單組 VCF。
- `shortest-one` 最短單組完整證明。
- 路線驗證、精簡與防守計算。

## 搜尋規則

每個攻方節點完成候選建立與排序後：

1. 直接連五候選為終局候選。
2. `defenseCount >= 2` 的活四或等價不可單點防守候選為終局候選。
3. 若沒有終局候選，照原本方式搜尋全部死四候選。
4. 若至少有一個終局候選，只保留全部終局候選，不再展開其後的單防死四候選。
5. 不得只保留第一個活四；同一盤面的所有直接五與活四勝點仍要列舉。

此規則屬於節點內候選支配剪枝，與下列剪枝互不衝突：

- 嚴格模式的完整同盤無解剪枝。
- 快速模式的勝型集合子集剪枝。

## 參數封裝

為維持既有 V3 Wasm ABI，選項與時間、節點限制共用 32-bit `maxNode` 參數：

- bit 31：封裝標記。
- bit 30：活四不搜後續。
- bit 10..29：時間限制，單位為秒；`0` 表示不限。
- bit 0..9：節點限制，單位為百萬；`0` 表示不限。

時間欄位最大值為 `1,048,575` 秒。舊橋接使用的 `0xffffffff` 不限節點值，啟用此選項時必須先正規化成新版封裝格式。

## 驗收

至少確認：

- 未勾選時，快速與嚴格模式維持原本路線集合。
- 勾選後，已有活四的節點不再出現先走死四再轉攻的路線。
- 同一節點有多個活四時，全部活四勝點仍會回傳。
- 沒有活四時，原本死四 VCF 搜尋不受影響。
- `single` 與 `shortest-one` 的結果及節點數不受影響。
- 時間與節點為 `0` 時仍代表不限制。
- C++ 自我測試、Native 編譯、Wasm ABI 與 JavaScript 語法檢查皆通過。