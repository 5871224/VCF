# 題目產生器死四連五點 N 點規格

> 本文件描述新死四候選建立後，連五點的正式 N 點處理規則。

## 1. 設定時機

- 每個新死四候選完成棋型確認後、回傳候選之前，必須立即處理本層連五點。
- 不得等到 VCF 搜尋、路線重播、候選驗證通過或最終答案建立後才補設。
- 後續自動封鎖較短 VCF、只保留目標 VCF、活三修補及補齊黑白子數，都必須使用已更新的 `nMask`。

## 2. N 點內容

- 一般死四的 `fivePoint` 設為 `NO_BLACK | NO_WHITE`。
- 同線四四的 `lineFivePoints` 可能包含兩個不同連五點，兩點都必須設為 `NO_BLACK | NO_WHITE`。
- `fivePoint` 與 `lineFivePoints` 重複時只處理一次。
- 不得改動無關空點的 N 點狀態。

## 3. 後續限制

連五點設為雙方 N 點後：

- 守方自動補守不得選到該點。
- 封鎖其他 VCF 不得選到該點。
- 補齊黑白子數不得選到該點。
- 後續新增外層死四時，必須繼承該 N 點。
- 白方抓禁手最內層的黑方禁點仍依既有規格保持雙方 N 點。

## 4. 驗收

1. 一般死四候選回傳時，`nMask[fivePoint]` 同時包含黑方與白方 N 位元。
2. 同線四四候選的兩個 `lineFivePoints` 都同時包含黑方與白方 N 位元。
3. 無關位置的 `nMask` 不變。
4. 補守候選篩選不再嘗試死四連五點。
5. `makevcf-generator-reuse-bonus.js` 通過 JavaScript 語法檢查。
