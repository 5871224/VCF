# VCF 專案現行開發規則

## 文件維護

- 每次修改功能、介面、搜尋邏輯、參數、預設值、資料格式或部署方式，必須在同一批變更中更新相關 Markdown 文件。
- Markdown 文件只保留目前正式規格，不保留修改歷程、版本沿革、淘汰方案、過時預設值或過渡作法。
- 新規格取代舊規格時，直接改寫或刪除舊內容，不以附註方式同時保留互相衝突的規則。
- 修改完成前，必須搜尋根目錄及相關子目錄的 Markdown，確認名稱、數值、執行路徑與正式程式一致。
- GitHub Actions 產生程式提交後，必須再以一般 `main` 分支提交或手動執行 `workflow_dispatch` 觸發 Pages 部署，避免 `GITHUB_TOKEN` 推送未產生新的部署流程。

## 正式入口與執行路徑

唯一對外正式網址為：

```text
https://5871224.github.io/VCF/
```

根網址直接載入新版 Bitboard 工作台，不得轉址到 `/rapfi/`，也不得在根頁保留舊版引擎或另一套介面。`/rapfi/` 只作為引擎、Worker、UI 模組與實驗室資源目錄。

正式搜尋路徑：

```text
/VCF/index.html（由 makevcf.html 建置）
  → rapfi/engine/vcf-bitboard-engine.js / .wasm
  → rapfi/vcf-bitboard-main.js
  → rapfi/vcf-bitboard-worker.js
```

`makevcf.html` 是根頁與建置流程共用的工作台來源，不是另一套正式產品頁。正式功能、版面與腳本只維護一份來源。

題目產生器使用獨立 Worker：

```text
makevcf-generator-core.js
  → rapfi/vcf-bitboard-worker.js
  → rapfi/engine/vcf-bitboard-engine.js / .wasm
```

修改根工作台、`/rapfi/` 資源或題目產生器前，必須先閱讀：

- [`新版Bitboard VCF規格.MD`](新版Bitboard%20VCF規格.MD)
- [`題目產生器規格.MD`](題目產生器規格.MD)
- [`禁手判斷規格.MD`](禁手判斷規格.MD)（修改棋型、長連或禁手時）

## 規則值

規則值固定為：

- `0 = 自由`：黑白長連皆算勝；黑白棋型皆取 9 格。
- `1 = 無禁`：黑白皆無禁手，長連不算勝也不算負；黑白都取 11 格。
- `2 = 有禁`：連珠規則，黑棋有禁手；黑棋取 11 格、白棋取 9 格。

不得使用 `Number(rules) || 2` 等會把自由規則 `0` 誤判為預設值的寫法。每個 Worker 搜尋、精簡及防守請求都必須明確附帶目前規則。

## Bitboard 工作台不可破壞的規則

- 單組 VCF 找到第一組立即返回，使用專用 256K 四路精確同型表。
- `multi` 固定最多回傳 64 組，先以原始攻守手順做子集去重，再精簡保留路線。
- 工作台「同型-活四取前一手」預設勾選；勾選時，最後以活四取勝的路線在同型 key 中移除該活四手，未勾選時把活四手保留在同型 key 中。
- 題目產生器固定使用：多手路線最後一手為活四時取前一手去重；第一手就是活四時不取前一手，直接依活四落子點去重。去重只改比較 key，答案與防點計算仍使用完整路線。
- `strict` 只以完整黑白棋盤完全相同判定同型。
- `fast` 在完整同盤無解剪枝之外，才使用已完整證明的勝型子集剪枝。
- `shortest` 逐奇數 ply 加深，回傳第一個有解深度的最短層多組路線。
- `shortest-one` 全程單執行緒；先找目前最佳路線，再以 `bestPly - 2` 排除更短路線。限制中止時可保留目前最佳路線，但不得宣告已證明最短。
- 補子搜尋只保留「最速」與「最短」：最速逐點使用 `single`，最短逐點使用 `shortest-one`。
- 補子搜尋完成後，在所有顯示 VCF 步數的落點中，以圓圈標示數字最大的落點；`4`、`5` 棋型標示不參與圈選。
- 多組、最短、補子搜尋及題目產生器共用工作台的「時間限制」與「節點限制」輸入。時間單位為秒、節點單位為百萬，`0` 代表不限制。
- 多組型搜尋以 32-bit `maxNode` 封裝時間與節點限制；不得再以固定 5,000,000 取代使用者輸入。
- 時間每 524,288 節點檢查一次。因時間或節點限制中止時，保留已找到結果，但提示必須明確標示搜尋未完整。
- 搜尋完成提示格式固定為 `7.4351 秒，30,123,123 節點，4,123,123 節點/秒`。
- 「顯示禁手」只在有禁規則下標示黑棋禁點；長連顯示 `6`、四四顯示 `44`、三三顯示 `33`，盤面或規則改變後必須重新計算。

## 題目產生器不可破壞的規則

- 題目產生器不得共用、中止或重新啟動主分析 Worker。
- 題目可指定 1～10 步；步數是最後連五前攻方實際落子的手數。
- 一般候選使用 `shortest`；「只保留目標 VCF」使用 `multi`，最多 64 組。
- `shortest` 與 `multi` 都依「多組剪枝」下拉選單選擇 `fast` 或 `strict`。
- 最大深度固定為 `2 × 目標步數 + 1` ply。
- 題目產生開始時讀取工作台「時間限制」及「節點限制」，整次產生使用同一份快照；產生期間必須停用這兩個輸入及剪枝下拉選單。
- 時間或節點輸入為 `0` 時代表不限制，不得回退成 500 萬節點。
- 唯一題搜尋只要 `aborted=true`、回傳 64 組、達目前時間限制或達目前節點限制，就不得宣告唯一。
- 未啟用「補齊黑白子數」時，存在比目標短的 VCF 就淘汰候選。
- 啟用「補齊黑白子數」時，先封鎖目前最短 VCF，再重新搜尋，直到最短層等於目標並符合預期完成盤面。
- 自動封鎖每層最多嘗試 6 個防守點，每個候選最多展開 42 個狀態。
- 最後補齊子數每層最多嘗試 10 個點，最多展開 140 個狀態。
- 黑方有禁規則的活三初始材料必須加入額外黑方 N 點，避免使用非法外部延伸。
- 新死四若產生活三，只能在可用 X 點補守；枚舉所有可用 X 組合後，只保留補守子數最少且已消除活三的方案。
- 「沿用棋子加成」同時計算沿用的攻方棋，以及 X 點或五點原有的守方棋；A 不計入。
- 「棋子集中加成」以目前盤面全部黑白棋的座標平均值為分布中心，依新死四模板中央點與分布中心的距離計分。
- 未勾選「依加成高低排序」時依權重隨機排序；勾選時依總權重由高到低驗證，同權重隨機排列。
- 結果內的 `nodeCount` 是最後一次成功驗證搜尋的節點數，不是整次產生期間的累計節點數；文件與介面不得誤稱為總節點數。

## 題庫

- 題庫唯一資料來源為 Supabase。
- 題庫只保存 225 格 `board` 與 `attacker`；不得保留 `localStorage` 題庫、搬移流程或其他儲存相容層。
- 載入題目時共用目前工作台規則；若未來要保存規則，必須先同步修改資料表、讀寫程式與文件。

## 驗收

搜尋核心變更至少必須通過：

- Native C++ 編譯與自我測試。
- LTO SIMD Wasm 編譯。
- Wasm ABI 測試。
- JavaScript 語法檢查。
- 相同盤面與相同限制下的結果、節點數及耗時比較。

題目產生器變更至少必須確認：

- 所有 `makevcf-generator-*.js` 通過語法檢查。
- 自由、無禁與有禁規則分別以 `0`、`1`、`2` 傳入每一個 Worker 請求。
- 一般驗證送出 `shortest`，唯一題驗證送出 `multi`。
- 時間、節點、深度、64 組上限及剪枝選擇正確傳入。
- `0` 限制不會被固定預設值覆蓋。
- 題目產生期間不能切換規則、攻方、步數、搜尋限制、剪枝及權重控制項。
- 根網址直接載入新版 Bitboard 工作台，網址不發生轉址，也不顯示舊版介面。
- 手機版操作正常，相關 Markdown 已同步且沒有殘留舊規格。
